---
'@embedpdf/plugin-measurement': patch
---

Key the per-document driver by the capability instance instead of the document id, so two kernels holding the same document id keep independent drivers.
