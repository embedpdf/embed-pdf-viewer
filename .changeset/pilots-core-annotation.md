---
'@embedpdf/core-annotation': minor
---

Add the `createAnnot` message: programmatic creation from page-space geometry that mints the same optimistic annotation and `create` effect a draw tool commits, from the preset's defaults with property overrides layered on top.
