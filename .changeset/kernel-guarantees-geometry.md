---
'@embedpdf/core-geometry': minor
---

Add `pageSpace(crop)`, the one owner of page ↔ PDF conversion for a page (points, rectangles and quads both ways), plus the `PagePoint`, `PageRect` and `PageQuad` aliases for page space.
