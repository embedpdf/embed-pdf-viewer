---
'@embedpdf/core-annotation': minor
---

Model records, drafts, pointer input, render items, chrome nodes and draft/selection anchors identify their page with `page: PageRef`; hit-testing, snapping and view projection take a `PageRef`.
