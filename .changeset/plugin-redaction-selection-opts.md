---
'@embedpdf/plugin-redaction': patch
---

Updated to use the new `enableForMode` signature with options object. Configured redaction mode to suppress selection layer rects since the redaction plugin renders its own selection visualization.
