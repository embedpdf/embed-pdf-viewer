---
'@embedpdf/plugin-page-edit': minor
---

`rotateBy(page)`, `setRotation`, `move` and `delete(pages)` take `PageRef` addresses; `PagePlacement` anchors on `{ before: PageRef }` / `{ after: PageRef }` and is exported from the contract entry.
