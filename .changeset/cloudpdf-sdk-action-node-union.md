---
'@cloudpdf/sdk': minor
---

Regenerated types for annotation and form responses: action-tree roots are now a payload-carrying union (destinations, URIs, named actions, Hide targets, ResetForm state, file specs). Recursive `/Next` chain elements remain `unknown[]` — a Fern limitation of the emitted recursive schema, not missing data.
