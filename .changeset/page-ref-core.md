---
'@embedpdf/core': minor
---

Re-export `PageRef`, `toPageRef`, `pageRefsEqual`, `encodePageKey` and `decodePageKey` from the kernel entry, and describe `DocumentMeta.pages[].ref` as the durable page identity.
