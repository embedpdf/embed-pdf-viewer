---
'@embedpdf/react': minor
---

`useMetadata` returns `status` and `refresh`; `useSearchState` follows the new search getters; `SearchLayer` reads reference-stable per-page hits; new `useMetadataEvent` and `useSearchEvent`.
