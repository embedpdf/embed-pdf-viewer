---
'@embedpdf/core': minor
---

`composeApi(id, slices)` builds a capability from area slices and refuses a member two slices define; `ctx.geometry.tryForPage(ref)` answers null for a page not in the document (beside the throwing `forPage`); the new `@embedpdf/core/testing` entry ships `createTestContext`, a kernel-shaped `ControllerContext` for plugin unit tests.
