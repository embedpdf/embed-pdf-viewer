---
'@embedpdf/plugin-stage': patch
---

The main stage lens feeds the action engine's lifecycle coordinator: `{ currentPon, visiblePons, placed, cause }` page-state reports on placement and change (pons, not indexes — reorder-safe), with motion cause tagged at the doors (arrival/reveal = programmatic, direct camera manipulation = user) as the cascade budget's fuel.
