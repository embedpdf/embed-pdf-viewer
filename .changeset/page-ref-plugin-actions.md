---
'@embedpdf/plugin-actions': minor
---

Action sources, triggers, hover targets and page-state reports identify pages with `page: PageRef` (`currentPage`, `visiblePages`); annotation commit entries carry `page`.
