---
'@embedpdf/plugin-selection': minor
---

`select({ page, start, count })`, `segmentsForPage`, `rectsForPage` and the host gesture methods take a `PageRef`; snapshots, endpoints, ranges, menu anchors, glyph pointers and `selectedPages()` carry refs.
