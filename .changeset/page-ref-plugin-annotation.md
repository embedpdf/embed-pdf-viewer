---
'@embedpdf/plugin-annotation': minor
---

`create(page, draft)`, `list(page)`, stamp placement, hover and every host method that names a page take a `PageRef`; selection and creation-draft anchors, text items, tool ghosts, calibration captures and recalibration reports carry `page`.
