---
'@embedpdf/engine-services': minor
---

Resolve `PageRef` addresses at the worker boundary and emit the new page-identity record shapes: page state, revision tokens, annotation refs and DTOs, widget refs (`null` when unplaced), destinations, search matches, named-page targets, and the page fields of flatten, redaction, insert and scale results.
