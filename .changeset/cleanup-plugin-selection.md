---
'@embedpdf/plugin-selection': patch
---

A page's text geometry is also re-read after an annotation flatten painted into it, so text that a flattened free-text note adds to the page becomes selectable.
