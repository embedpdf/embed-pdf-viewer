---
'@embedpdf/plugin-annotation': patch
---

Add `isRotatable: false` to text markup annotation tools (highlight, underline, strikeout, squiggly) to explicitly prevent rotation on these text-anchored annotations.
