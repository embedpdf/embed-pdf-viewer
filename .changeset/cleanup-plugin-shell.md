---
'@embedpdf/plugin-shell': minor
---

The shell's surface and menu changes are named state transitions and its events are derived from them. The `/internal` entry is removed, and `ShellState` and `ShellAction` are no longer exported.
