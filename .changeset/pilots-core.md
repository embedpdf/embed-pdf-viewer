---
'@embedpdf/core': minor
---

`ResourceStatus`, `ChangeOrigin` and `originOf` join the kernel vocabulary; `PluginErrorInfo` / `toPluginErrorInfo` give events and state a serializable error summary; a lane cancellation carries `{ runId, reason }` in its details.
