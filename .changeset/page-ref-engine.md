---
'@embedpdf/engine': minor
---

Page handles, page services and weak-edit sessions take and expose `PageRef` addresses; local document events carry `page`/`pages` refs instead of bare page object numbers.
