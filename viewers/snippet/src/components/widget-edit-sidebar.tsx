import { h } from 'preact';

export interface WidgetEditSidebarProps {
  documentId: string;
}

export const WidgetEditSidebar = ({ documentId }: WidgetEditSidebarProps) => {
  return (
    <div>
      <h1>Widget Edit Sidebar</h1>
    </div>
  );
};
