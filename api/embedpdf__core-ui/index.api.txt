# @embedpdf/core-ui — .

AddItemSpec [InterfaceDeclaration] (from packages/core/ui/src/transforms.ts): export interface AddItemSpec{ readonly bar: string; readonly section: keyof BarSections; readonly group: string; readonly item: BarChild; readonly at?: number; }
BarChild [TypeAliasDeclaration] (from packages/core/ui/src/schema.ts): export type BarChild = BarItem | CustomItem | string;
BarGroup [InterfaceDeclaration] (from packages/core/ui/src/schema.ts): export interface BarGroup{ readonly id: string; readonly labelKey?: string; readonly role?: 'buttons' | 'tabs'; readonly shed?: boolean; readonly collapse?: 'menu' | 'select'; readonly importance?: Importance; readonly items: readonly BarChild[]; }
BarItem [InterfaceDeclaration] (from packages/core/ui/src/schema.ts): export interface BarItem{ readonly command: string; readonly variants?: readonly Variant[]; readonly importance?: Importance; }
BarSchema [InterfaceDeclaration] (from packages/core/ui/src/schema.ts): export interface BarSchema{ readonly id: string; readonly sections: BarSections; }
BarSections [InterfaceDeclaration] (from packages/core/ui/src/schema.ts): export interface BarSections{ readonly start?: readonly BarGroup[]; readonly center?: readonly BarGroup[]; readonly end?: readonly BarGroup[]; }
ChromeHelpers [TypeAliasDeclaration] (from packages/core/ui/src/helpers.ts): export type ChromeHelpers = typeof chromeHelpers;
ChromeSchema [InterfaceDeclaration] (from packages/core/ui/src/schema.ts): export interface ChromeSchema{ readonly bars: Readonly<Record<string, BarSchema>>; readonly modeBars?: Readonly<Record<string, BarSchema>>; readonly menus?: Readonly<Record<string, MenuSchema>>; readonly strips?: Readonly<Record<string, BarSchema>>; readonly frame?: FrameSchema; }
CustomItem [InterfaceDeclaration] (from packages/core/ui/src/schema.ts): export interface CustomItem{ readonly slot: string; readonly variants?: readonly string[]; readonly terminal: string; readonly importance?: Importance; }
DEFAULT_IMPORTANCE [VariableDeclaration] (from packages/core/ui/src/schema.ts): DEFAULT_IMPORTANCE: Importance = 3
FitMetrics [InterfaceDeclaration] (from packages/core/ui/src/solver.ts): export interface FitMetrics{ unit(key: string, variant: string): number | undefined; groupCollapsed(groupId: string): number | undefined; groupTrigger(groupId: string): number | undefined; readonly overflowTrigger: number; readonly gap: number; readonly separator: number; }
FitResult [InterfaceDeclaration] (from packages/core/ui/src/solver.ts): export interface FitResult{ readonly units: ReadonlyMap<string, UnitAssignment>; readonly groups: ReadonlyMap<string, GroupAssignment>; readonly hasOverflow: boolean; readonly width: number; readonly complete: boolean; }
FrameSchema [InterfaceDeclaration] (from packages/core/ui/src/schema.ts): export interface FrameSchema{ readonly toolbar?: 'top' | 'bottom'; readonly tabs?: 'always' | 'multiple' | 'never'; readonly header?: boolean; }
GroupAssignment [InterfaceDeclaration] (from packages/core/ui/src/solver.ts): export interface GroupAssignment{ readonly shedCount: number; readonly collapsed: boolean; readonly overflowed: boolean; }
Importance [TypeAliasDeclaration] (from packages/core/ui/src/schema.ts): export type Importance = 1 | 2 | 3 | 4 | 5;
KeyStroke [InterfaceDeclaration] (from packages/core/ui/src/shortcuts.ts): export interface KeyStroke{ readonly key: string; readonly code?: string; readonly ctrlKey: boolean; readonly metaKey: boolean; readonly altKey: boolean; readonly shiftKey: boolean; }
MenuSchema [InterfaceDeclaration] (from packages/core/ui/src/schema.ts): export interface MenuSchema{ readonly id: string; readonly sections: readonly MenuSection[]; }
MenuSection [InterfaceDeclaration] (from packages/core/ui/src/schema.ts): export interface MenuSection{ readonly labelKey?: string; readonly items: readonly string[]; }
NormalizedBar [InterfaceDeclaration] (from packages/core/ui/src/schema.ts): export interface NormalizedBar{ readonly id: string; readonly sections: readonly NormalizedSection[]; }
NormalizedGroup [InterfaceDeclaration] (from packages/core/ui/src/schema.ts): export interface NormalizedGroup{ readonly id: string; readonly labelKey?: string; readonly role: 'buttons' | 'tabs'; readonly shed: boolean; readonly collapse?: 'menu' | 'select'; readonly importance: Importance; readonly units: readonly NormalizedUnit[]; }
NormalizedSection [InterfaceDeclaration] (from packages/core/ui/src/schema.ts): export interface NormalizedSection{ readonly name: 'start' | 'center' | 'end'; readonly groups: readonly NormalizedGroup[]; }
NormalizedUnit [TypeAliasDeclaration] (from packages/core/ui/src/schema.ts): export type NormalizedUnit = |{ readonly kind: 'command'; readonly key: string; readonly command: string; readonly variants: readonly string[]; readonly importance: Importance; } |{ readonly kind: 'custom'; readonly key: string; readonly slot: string; readonly variants: readonly string[]; readonly terminal: string; readonly importance: Importance; } ;
OverflowRow [TypeAliasDeclaration] (from packages/core/ui/src/projection.ts): export type OverflowRow = |{ readonly type: 'command'; readonly command: string} |{ readonly type: 'submenu'; readonly command: string; readonly menu: string} ;
OverflowSection [InterfaceDeclaration] (from packages/core/ui/src/projection.ts): export interface OverflowSection{ readonly labelKey?: string; readonly role?: 'radio'; readonly rows: readonly OverflowRow[]; }
PINNED [VariableDeclaration] (from packages/core/ui/src/schema.ts): PINNED: Importance = 5
ParsedShortcut [InterfaceDeclaration] (from packages/core/ui/src/shortcuts.ts): export interface ParsedShortcut{ readonly key: string; readonly mod: boolean; readonly ctrl: boolean; readonly meta: boolean; readonly alt: boolean; readonly shift: boolean; }
ResolveMenuTarget [TypeAliasDeclaration] (from packages/core/ui/src/projection.ts): export type ResolveMenuTarget = (commandId: string) =>{ menu?: string} | null;
StripGroup [InterfaceDeclaration] (from packages/core/ui/src/projection.ts): export interface StripGroup{ readonly id: string; readonly labelKey?: string; readonly commands: readonly string[]; }
UnitAssignment [TypeAliasDeclaration] (from packages/core/ui/src/solver.ts): export type UnitAssignment = |{ readonly kind: 'variant'; readonly variant: string} |{ readonly kind: 'shed'} |{ readonly kind: 'collapsed'} |{ readonly kind: 'overflow'} ;
Variant [TypeAliasDeclaration] (from packages/core/ui/src/schema.ts): export type Variant = 'icon+label' | 'icon' | 'label';
addItem [FunctionDeclaration] (from packages/core/ui/src/transforms.ts): export function addItem(schema: ChromeSchema, spec: AddItemSpec): ChromeSchema{ const hits = locate(schema, spec.bar); if (hits.length === 0) throw new Error(`[ui-core] addItem: no bar "${ spec.bar} " in bars/modeBars/strips`); if (hits.length > 1) throw new Error( `[ui-core] addItem: bar id "${ spec.bar} " is ambiguous (${ hits.map(([c]) => c).join(', ')} )`, ); const [coll, key] = hits[0]; const bar = schema[coll]![key]; for (const name of SECTION_NAMES){ if (name === spec.section) continue; if (bar.sections[name]?.some((g) => g.id === spec.group)) throw new Error( `[ui-core] addItem: group "${ spec.group} " lives in section "${ name} ", not "${ spec.section} "`, ); } const groups = bar.sections[spec.section] ?? []; const idx = groups.findIndex((g) => g.id === spec.group); const nextGroups = idx === -1 ? [...groups, makeGroup(spec.group, [spec.item])] : groups.map((g, i) =>{ if (i !== idx) return g; const items = [...g.items]; items.splice(spec.at ?? items.length, 0, spec.item); return{ ...g, items} ; } ); return withBar(schema, coll, key, { ...bar, sections:{ ...bar.sections, [spec.section]: nextGroups} , } ); }
chromeHelpers [VariableDeclaration] (from packages/core/ui/src/helpers.ts): chromeHelpers ={ addItem, removeItems, replaceItem, item, group, custom, defineChrome, } as const
custom [FunctionDeclaration] (from packages/core/ui/src/schema.ts): export function custom(slot: string, opts: Omit<CustomItem, 'slot'>): CustomItem{ return{ slot, ...opts} ; }
defineChrome [FunctionDeclaration] (from packages/core/ui/src/schema.ts): export function defineChrome<T extends ChromeSchema>(chrome: T): T{ for (const bar of Object.values(chrome.bars)) normalizeBar(bar); for (const bar of Object.values(chrome.modeBars ??{ } )) normalizeBar(bar); for (const strip of Object.values(chrome.strips ??{ } )) normalizeBar(strip); return chrome; }
filterBar [FunctionDeclaration] (from packages/core/ui/src/schema.ts): export function filterBar( bar: NormalizedBar, keep: (unit: NormalizedUnit) => boolean, ): NormalizedBar{ return{ id: bar.id, sections: bar.sections.map((s) => ({ name: s.name, groups: s.groups.map((g) => ({ ...g, units: g.units.filter(keep)} )), } )), } ; }
formatShortcut [FunctionDeclaration] (from packages/core/ui/src/shortcuts.ts): export function formatShortcut(shortcut: string, opts:{ isMac: boolean} ): string{ const parsed = parseShortcut(shortcut); const key = parsed.key.length === 1 ? parsed.key.toUpperCase() : capitalize(parsed.key); if (opts.isMac){ const mods = [ parsed.ctrl ? '⌃' : '', parsed.alt ? '⌥' : '', parsed.shift ? '⇧' : '', parsed.meta || parsed.mod ? '⌘' : '', ].join(''); return `${ mods} ${ key} `; } const mods = [ parsed.ctrl || parsed.mod ? 'Ctrl' : '', parsed.meta ? 'Meta' : '', parsed.alt ? 'Alt' : '', parsed.shift ? 'Shift' : '', ].filter(Boolean); return [...mods, key].join('+'); }
group [FunctionDeclaration] (from packages/core/ui/src/schema.ts): export function group(id: string, items: readonly BarChild[]): BarGroup;
item [FunctionDeclaration] (from packages/core/ui/src/schema.ts): export function item(command: string, opts?: Omit<BarItem, 'command'>): BarItem{ return{ command, ...opts} ; }
matchShortcut [FunctionDeclaration] (from packages/core/ui/src/shortcuts.ts): export function matchShortcut( parsed: ParsedShortcut, stroke: KeyStroke, opts:{ isMac: boolean} , ): boolean{ const wantCtrl = parsed.ctrl || (parsed.mod && !opts.isMac); const wantMeta = parsed.meta || (parsed.mod && opts.isMac); if (stroke.ctrlKey !== wantCtrl) return false; if (stroke.metaKey !== wantMeta) return false; if (stroke.altKey !== parsed.alt) return false; if (parsed.shift && !stroke.shiftKey) return false; if (!parsed.shift && stroke.shiftKey && parsed.key.length > 1) return false; const key = stroke.key.toLowerCase(); if (key === parsed.key) return true; return ( parsed.key.length > 1 && stroke.code !== undefined && stroke.code.toLowerCase() === parsed.key ); }
normalizeBar [FunctionDeclaration] (from packages/core/ui/src/schema.ts): export function normalizeBar(bar: BarSchema): NormalizedBar{ const seen = new Set<string>(); const groupIds = new Set<string>(); const sections: NormalizedSection[] = []; for (const name of ['start', 'center', 'end'] as const){ const groups = bar.sections[name]; if (!groups || groups.length === 0) continue; sections.push({ name, groups: groups.map((g) =>{ if (groupIds.has(g.id)) throw new Error(`[ui-core] duplicate group in bar: ${ g.id} `); groupIds.add(g.id); return{ id: g.id, labelKey: g.labelKey, role: g.role ?? 'buttons', shed: g.shed ?? false, collapse: g.collapse, importance: g.importance ?? DEFAULT_IMPORTANCE, units: g.items.map((c) => normalizeChild(g.id, c, seen)), } ; } ), } ); } return{ id: bar.id, sections} ; }
parseShortcut [FunctionDeclaration] (from packages/core/ui/src/shortcuts.ts): export function parseShortcut(shortcut: string): ParsedShortcut{ const tokens = shortcut.split('+').map((t) => t.trim()); const keyToken = tokens[tokens.length - 1] === '' ? '+' : tokens[tokens.length - 1]; const parsed ={ key: keyToken.toLowerCase(), mod: false, ctrl: false, meta: false, alt: false, shift: false, } ; for (const token of tokens.slice(0, -1)){ const t = token.toLowerCase(); if (t === '') continue; if (!MODIFIERS.has(t)) throw new Error(`[ui-core] unknown modifier "${ token} " in "${ shortcut} "`); if (t === 'mod') parsed.mod = true; else if (t === 'ctrl' || t === 'control') parsed.ctrl = true; else if (t === 'meta' || t === 'cmd' || t === 'command') parsed.meta = true; else if (t === 'alt' || t === 'option') parsed.alt = true; else parsed.shift = true; } if (parsed.key === '') throw new Error(`[ui-core] empty key in shortcut "${ shortcut} "`); return parsed; }
projectOverflow [FunctionDeclaration] (from packages/core/ui/src/projection.ts): export function projectOverflow( bar: NormalizedBar, fit: FitResult, resolve: ResolveMenuTarget, ): OverflowSection[]{ const sections: OverflowSection[] = []; for (const section of bar.sections){ for (const group of section.groups){ const rows = group.units .filter((u) => fit.units.get(u.key)?.kind === 'overflow') .map((u) => projectUnit(u, resolve)); if (rows.length === 0) continue; sections.push({ labelKey: group.labelKey, role: group.role === 'tabs' ? 'radio' : undefined, rows, } ); } } return sections; }
projectShed [FunctionDeclaration] (from packages/core/ui/src/projection.ts): export function projectShed( group: NormalizedGroup, fit: FitResult, resolve: ResolveMenuTarget, ): OverflowRow[]{ return group.units .filter((u) => fit.units.get(u.key)?.kind === 'shed') .map((u) => projectUnit(u, resolve)); }
projectStrip [FunctionDeclaration] (from packages/core/ui/src/projection.ts): export function projectStrip( bar: NormalizedBar, visible: (command: string) => boolean, ): StripGroup[]{ const groups: StripGroup[] = []; for (const section of bar.sections){ for (const group of section.groups){ const commands = group.units .map((u) => (u.kind === 'custom' ? u.terminal : u.command)) .filter(visible); if (commands.length === 0) continue; groups.push({ id: group.id, labelKey: group.labelKey, commands} ); } } return groups; }
removeItems [FunctionDeclaration] (from packages/core/ui/src/transforms.ts): export function removeItems(schema: ChromeSchema, ids: readonly string[]): ChromeSchema{ const gone = new Set(ids); const keep = (child: BarChild): boolean =>{ const cmd = commandOf(child); return cmd !== null ? !gone.has(cmd) : !gone.has((child as CustomItem).slot); } ; const filterGroups = (groups: readonly BarGroup[] | undefined) => groups?.map((g) => ({ ...g, items: g.items.filter(keep)} )).filter((g) => g.items.length > 0); const filterBarSchema = (bar: BarSchema): BarSchema => ({ ...bar, sections: Object.fromEntries( SECTION_NAMES.flatMap((name) =>{ const groups = filterGroups(bar.sections[name]); return groups && groups.length > 0 ? [[name, groups]] : []; } ), ), } ); const filterColl = <T extends Readonly<Record<string, BarSchema>> | undefined>(coll: T): T => coll && (Object.fromEntries(Object.entries(coll).map(([k, b]) => [k, filterBarSchema(b)])) as T); return{ ...schema, bars: filterColl(schema.bars)!, modeBars: filterColl(schema.modeBars), strips: filterColl(schema.strips), menus: schema.menus && Object.fromEntries( Object.entries(schema.menus).map(([k, menu]) => [ k, { ...menu, sections: menu.sections .map((s) => ({ ...s, items: s.items.filter((c) => !gone.has(c))} )) .filter((s) => s.items.length > 0), } , ]), ), } ; }
replaceItem [FunctionDeclaration] (from packages/core/ui/src/transforms.ts): export function replaceItem(schema: ChromeSchema, id: string, item: BarChild): ChromeSchema{ const replacementCommand = commandOf(item); const swap = (child: BarChild): BarChild => (commandOf(child) === id ? item : child); const mapBar = (bar: BarSchema): BarSchema => ({ ...bar, sections: Object.fromEntries( SECTION_NAMES.flatMap((name) =>{ const groups = bar.sections[name]; return groups ? [[name, groups.map((g) => ({ ...g, items: g.items.map(swap)} ))]] : []; } ), ), } ); const mapColl = <T extends Readonly<Record<string, BarSchema>> | undefined>(coll: T): T => coll && (Object.fromEntries(Object.entries(coll).map(([k, b]) => [k, mapBar(b)])) as T); return{ ...schema, bars: mapColl(schema.bars)!, modeBars: mapColl(schema.modeBars), strips: mapColl(schema.strips), menus: replacementCommand === null ? schema.menus : schema.menus && Object.fromEntries( Object.entries(schema.menus).map(([k, menu]) => [ k, { ...menu, sections: menu.sections.map((s) => ({ ...s, items: s.items.map((c) => (c === id ? replacementCommand : c)), } )), } , ]), ), } ; }
solve [FunctionDeclaration] (from packages/core/ui/src/solver.ts): export function solve(bar: NormalizedBar, metrics: FitMetrics, containerWidth: number): FitResult{ let pass = solvePass(bar, metrics, containerWidth, false); if (anyOverflow(pass.groups)){ pass = solvePass(bar, metrics, containerWidth, true); } const units = new Map<string, UnitAssignment>(); const groupAssignments = new Map<string, GroupAssignment>(); for (const gs of pass.groups){ const shedCount = gs.units.filter((u) => u.shed && !u.overflowed && !gs.collapsed).length; groupAssignments.set(gs.group.id, { shedCount, collapsed: gs.collapsed, overflowed: gs.overflowed, } ); for (const us of gs.units){ units.set( us.unit.key, us.overflowed ?{ kind: 'overflow'} : gs.collapsed ?{ kind: 'collapsed'} : us.shed ?{ kind: 'shed'} :{ kind: 'variant', variant: us.unit.variants[us.variantIndex]} , ); } } return{ units, groups: groupAssignments, hasOverflow: anyOverflow(pass.groups), width: pass.measured.width, complete: pass.measured.complete, } ; }
validateChrome [FunctionDeclaration] (from packages/core/ui/src/schema.ts): export function validateChrome(chrome: ChromeSchema, knownCommands: ReadonlySet<string>): string[]{ const problems: string[] = []; const checkBar = (bar: BarSchema, where: string) =>{ for (const section of normalizeBar(bar).sections) for (const g of section.groups) for (const u of g.units){ const refs = u.kind === 'command' ? [u.command] : [u.terminal]; for (const c of refs) if (!knownCommands.has(c)) problems.push(`${ where} : unknown command "${ c} "`); } } ; for (const [id, bar] of Object.entries(chrome.bars)) checkBar(bar, `bars.${ id} `); for (const [id, bar] of Object.entries(chrome.modeBars ??{ } )) checkBar(bar, `modeBars.${ id} `); for (const [id, strip] of Object.entries(chrome.strips ??{ } )) checkBar(strip, `strips.${ id} `); for (const [id, menu] of Object.entries(chrome.menus ??{ } )) for (const section of menu.sections) for (const c of section.items) if (!knownCommands.has(c)) problems.push(`menus.${ id} : unknown command "${ c} "`); return problems; }
