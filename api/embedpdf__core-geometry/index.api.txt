# @embedpdf/core-geometry — .

Mat2D [TypeAliasDeclaration]: export type Mat2D<From extends Space = Space, To extends Space = Space> = readonly [ number, number, number, number, number, number, ] &{ readonly __from?: From; readonly __to?: To; } ;
NO_FRAME [VariableDeclaration]: NO_FRAME: PageFrame ={ top: 0, right: 0, bottom: 0, left: 0}
PageFrame [InterfaceDeclaration]: export interface PageFrame{ top: number; right: number; bottom: number; left: number; }
PageGeometry [InterfaceDeclaration]: export interface PageGeometry{ readonly pdfToContent: Mat2D<'pdf', 'content'>; readonly contentToView: Mat2D<'content', 'view'>; readonly pdfToView: Mat2D<'pdf', 'view'>; readonly viewToPdf: Mat2D<'view', 'pdf'>; }
PageGeometryInput [InterfaceDeclaration]: export interface PageGeometryInput{ crop:{ left: number; bottom: number; right: number; top: number} ; rotation: PageRotation; userUnit: number; }
PagePoint [TypeAliasDeclaration] (from packages/core/geometry/src/page-space.ts): export type PagePoint = PointIn<'content'>;
PageQuad [TypeAliasDeclaration] (from packages/core/geometry/src/page-space.ts): export type PageQuad = QuadIn<'content'>;
PageRect [TypeAliasDeclaration] (from packages/core/geometry/src/page-space.ts): export type PageRect = RectIn<'content'>;
PageRotation [TypeAliasDeclaration]: export type PageRotation = 0 | 90 | 180 | 270;
PageSpace [InterfaceDeclaration] (from packages/core/geometry/src/page-space.ts): export interface PageSpace{ readonly crop: PdfEdges; readonly width: number; readonly height: number; pdfToPage(point:{ x: number; y: number} ): PagePoint; pageToPdf(point:{ x: number; y: number} ): PointIn<'pdf'>; pdfRectToPage(rect: PdfEdges): PageRect; pageRectToPdf(rect: Rect): PdfEdges; pdfQuadToPage(quad: Quad): PageQuad; pageQuadToPdf(quad: Quad): QuadIn<'pdf'>; }
PageTransform [InterfaceDeclaration]: export interface PageTransform{ readonly rotation: PageRotation; readonly viewWidth: number; readonly viewHeight: number; readonly contentWidth: number; readonly contentHeight: number; readonly deviceWidth: number; readonly deviceHeight: number; readonly renderScale: number; readonly viewScale: number; readonly baseScale: number; readonly zoom: number; toPixels(point: Point): Point; fromPixels(point: Point): Point; contentToView(point: Point): Point; contentToViewRect(rect: Rect): Rect; viewToContent(point: Point): Point; viewToContentRect(rect: Rect): Rect; readonly cssMatrix: string; }
PdfEdges [InterfaceDeclaration] (from packages/core/geometry/src/page-space.ts): export interface PdfEdges{ readonly left: number; readonly bottom: number; readonly right: number; readonly top: number; }
Point [InterfaceDeclaration]: export interface Point{ x: number; y: number; }
PointIn [TypeAliasDeclaration]: export type PointIn<S extends Space> = Point & SpaceBrand<S>;
Quad [InterfaceDeclaration]: export interface Quad{ p1: Point; p2: Point; p3: Point; p4: Point; }
QuadIn [TypeAliasDeclaration]: export type QuadIn<S extends Space> = Quad & SpaceBrand<S>;
Rect [InterfaceDeclaration]: export interface Rect{ x: number; y: number; width: number; height: number; }
RectIn [TypeAliasDeclaration]: export type RectIn<S extends Space> = Rect & SpaceBrand<S>;
Size [InterfaceDeclaration]: export interface Size{ width: number; height: number; }
Space [TypeAliasDeclaration]: export type Space = 'pdf' | 'content' | 'view' | 'screen';
TextQuad [InterfaceDeclaration]: export interface TextQuad{ upperStart: Point; upperEnd: Point; lowerStart: Point; lowerEnd: Point; }
TextQuadIn [TypeAliasDeclaration]: export type TextQuadIn<S extends Space> = TextQuad & SpaceBrand<S>;
addRotations [FunctionDeclaration]: export function addRotations(left: PageRotation, right: PageRotation): PageRotation{ return ((left + right) % 360) as PageRotation; }
angleOf [FunctionDeclaration]: export function angleOf(matrix: Mat2D): number{ return Math.atan2(matrix[1], matrix[0]); }
applyPoint [FunctionDeclaration]: export function applyPoint<F extends Space, T extends Space>( matrix: Mat2D<F, T>, point: PointIn<F>, ): PointIn<T>{ const [a, b, c, d, e, f] = matrix; return{ x: a * point.x + c * point.y + e, y: b * point.x + d * point.y + f} as PointIn<T>; }
applyQuad [FunctionDeclaration]: export function applyQuad<F extends Space, T extends Space>( matrix: Mat2D<F, T>, quad: QuadIn<F>, ): QuadIn<T>{ return{ p1: applyPoint(matrix, quad.p1 as PointIn<F>), p2: applyPoint(matrix, quad.p2 as PointIn<F>), p3: applyPoint(matrix, quad.p3 as PointIn<F>), p4: applyPoint(matrix, quad.p4 as PointIn<F>), } as QuadIn<T>; }
applyRect [FunctionDeclaration]: export function applyRect<F extends Space, T extends Space>( matrix: Mat2D<F, T>, rect: RectIn<F>, ): RectIn<T>{ const c0 = applyPoint(matrix, { x: rect.x, y: rect.y} as PointIn<F>); const c1 = applyPoint(matrix, { x: rect.x + rect.width, y: rect.y} as PointIn<F>); const c2 = applyPoint(matrix, { x: rect.x, y: rect.y + rect.height} as PointIn<F>); const c3 = applyPoint(matrix, { x: rect.x + rect.width, y: rect.y + rect.height} as PointIn<F>); const xs = [c0.x, c1.x, c2.x, c3.x]; const ys = [c0.y, c1.y, c2.y, c3.y]; const minX = Math.min(...xs); const minY = Math.min(...ys); return{ x: minX, y: minY, width: Math.max(...xs) - minX, height: Math.max(...ys) - minY, } as RectIn<T>; }
applyTextQuad [FunctionDeclaration]: export function applyTextQuad<F extends Space, T extends Space>( matrix: Mat2D<F, T>, textQuad: TextQuadIn<F>, ): TextQuadIn<T>{ return{ upperStart: applyPoint(matrix, textQuad.upperStart as PointIn<F>), upperEnd: applyPoint(matrix, textQuad.upperEnd as PointIn<F>), lowerStart: applyPoint(matrix, textQuad.lowerStart as PointIn<F>), lowerEnd: applyPoint(matrix, textQuad.lowerEnd as PointIn<F>), } as TextQuadIn<T>; }
boundsOfRects [FunctionDeclaration]: export function boundsOfRects(rects: readonly Rect[]): Rect | null{ if (rects.length === 0) return null; let x1 = Infinity; let y1 = Infinity; let x2 = -Infinity; let y2 = -Infinity; for (const rect of rects){ x1 = Math.min(x1, rect.x); y1 = Math.min(y1, rect.y); x2 = Math.max(x2, rect.x + rect.width); y2 = Math.max(y2, rect.y + rect.height); } return{ x: x1, y: y1, width: x2 - x1, height: y2 - y1} ; }
compose [FunctionDeclaration]: export function compose<A extends Space, B extends Space, C extends Space>( matrix: Mat2D<B, C>, inner: Mat2D<A, B>, ): Mat2D<A, C>{ const [a, b, c, d, e, f] = matrix; const [a2, b2, c2, d2, e2, f2] = inner; return [ a * a2 + c * b2, b * a2 + d * b2, a * c2 + c * d2, b * c2 + d * d2, a * e2 + c * f2 + e, b * e2 + d * f2 + f, ] as Mat2D<A, C>; }
deviceHeightForWidth [FunctionDeclaration]: export function deviceHeightForWidth(pageSize: Size, deviceWidth: number): number{ return Math.max(1, Math.round((deviceWidth * pageSize.height) / pageSize.width)); }
displaySize [FunctionDeclaration]: export function displaySize(content: Size, rotation: PageRotation): Size{ return isQuarterTurn(rotation) ?{ width: content.height, height: content.width} :{ width: content.width, height: content.height} ; }
edgesOfQuad [FunctionDeclaration] (from packages/core/geometry/src/page-space.ts): export function edgesOfQuad(quad: Quad): PdfEdges{ const xs = [quad.p1.x, quad.p2.x, quad.p3.x, quad.p4.x]; const ys = [quad.p1.y, quad.p2.y, quad.p3.y, quad.p4.y]; return{ left: Math.min(...xs), right: Math.max(...xs), bottom: Math.min(...ys), top: Math.max(...ys), } ; }
edgesOverlap [FunctionDeclaration] (from packages/core/geometry/src/page-space.ts): export function edgesOverlap(left: PdfEdges, right: PdfEdges): boolean{ return ( Math.min(left.right, right.right) > Math.max(left.left, right.left) && Math.min(left.top, right.top) > Math.max(left.bottom, right.bottom) ); }
identity [FunctionDeclaration]: export function identity<S extends Space>(): Mat2D<S, S>{ return [1, 0, 0, 1, 0, 0] as Mat2D<S, S>; }
intersectRects [FunctionDeclaration]: export function intersectRects(left: Rect, right: Rect): Rect{ const x = Math.max(left.x, right.x); const y = Math.max(left.y, right.y); return{ x, y, width: Math.max(0, Math.min(left.x + left.width, right.x + right.width) - x), height: Math.max(0, Math.min(left.y + left.height, right.y + right.height) - y), } ; }
invert [FunctionDeclaration]: export function invert<F extends Space, T extends Space>(matrix: Mat2D<F, T>): Mat2D<T, F>{ const [a, b, c, d, e, f] = matrix; const det = a * d - b * c; if (det === 0) throw new Error('Mat2D.invert: non-invertible matrix (det = 0)'); const ia = d / det; const ib = -b / det; const ic = -c / det; const id = a / det; return [ia, ib, ic, id, -(ia * e + ic * f), -(ib * e + id * f)] as Mat2D<T, F>; }
isQuarterTurn [FunctionDeclaration]: export function isQuarterTurn(rotation: PageRotation): boolean{ return rotation === 90 || rotation === 270; }
matrixToCss [FunctionDeclaration]: export function matrixToCss(matrix: Mat2D): string{ const [a, b, c, d, e, f] = matrix; return `matrix(${ a} , ${ b} , ${ c} , ${ d} , ${ e} , ${ f} )`; }
normalizeQuad [FunctionDeclaration]: export function normalizeQuad(quad: Quad): TextQuad{ const zigzag: QuadCornerList = [quad.p1, quad.p2, quad.p3, quad.p4]; if (zigzagWellFormed(zigzag)){ return{ upperStart: quad.p1, upperEnd: quad.p2, lowerStart: quad.p3, lowerEnd: quad.p4} ; } const ring: QuadCornerList = [quad.p1, quad.p2, quad.p4, quad.p3]; if (zigzagWellFormed(ring)){ return{ upperStart: quad.p1, upperEnd: quad.p2, lowerStart: quad.p4, lowerEnd: quad.p3} ; } const points = [quad.p1, quad.p2, quad.p3, quad.p4].map((point) => finitePoint(point) ? point :{ x: 0, y: 0} , ); const cx = (points[0].x + points[1].x + points[2].x + points[3].x) / 4; const cy = (points[0].y + points[1].y + points[2].y + points[3].y) / 4; const ringSorted = [...points].sort( (left, right) => Math.atan2(left.y - cy, left.x - cx) - Math.atan2(right.y - cy, right.x - cx), ); let upperEdge = 0; let bestY = Infinity; for (let i = 0; i < 4; i++){ const midY = (ringSorted[i].y + ringSorted[(i + 1) % 4].y) / 2; if (midY < bestY){ bestY = midY; upperEdge = i; } } const upperFirst = ringSorted[upperEdge]; const upperSecond = ringSorted[(upperEdge + 1) % 4]; const lowerSecond = ringSorted[(upperEdge + 2) % 4]; const lowerFirst = ringSorted[(upperEdge + 3) % 4]; const startFirst = upperFirst.x <= upperSecond.x; return startFirst ?{ upperStart: upperFirst, upperEnd: upperSecond, lowerStart: lowerFirst, lowerEnd: lowerSecond, } :{ upperStart: upperSecond, upperEnd: upperFirst, lowerStart: lowerSecond, lowerEnd: lowerFirst, } ; }
pageGeometry [FunctionDeclaration]: export function pageGeometry(input: PageGeometryInput, zoom: number): PageGeometry{ const{ crop, rotation, userUnit} = input; const pxPerPoint = zoom * userUnit; const pdfToContent = pdfToContentMatrix(crop); const Wc = (crop.right - crop.left) * pxPerPoint; const Hc = (crop.top - crop.bottom) * pxPerPoint; const contentToView = rotateScaleMatrix(pxPerPoint, Wc, Hc, rotation) as Mat2D<'content', 'view'>; const pdfToView = compose(contentToView, pdfToContent); return{ pdfToContent, contentToView, pdfToView, viewToPdf: invert(pdfToView)} ; }
pageSpace [FunctionDeclaration] (from packages/core/geometry/src/page-space.ts): export function pageSpace(crop: PdfEdges): PageSpace{ const toPage: Mat2D<'pdf', 'content'> = pdfToContentMatrix(crop); const toPdf: Mat2D<'content', 'pdf'> = invert(toPage); return{ crop, width: crop.right - crop.left, height: crop.top - crop.bottom, pdfToPage: (point) => applyPoint(toPage, point as PointIn<'pdf'>), pageToPdf: (point) => applyPoint(toPdf, point as PointIn<'content'>), pdfRectToPage: (rect) => ({ x: rect.left - crop.left, y: crop.top - rect.top, width: rect.right - rect.left, height: rect.top - rect.bottom, } ) as PageRect, pageRectToPdf: (rect) => ({ left: rect.x + crop.left, right: rect.x + rect.width + crop.left, top: crop.top - rect.y, bottom: crop.top - (rect.y + rect.height), } ), pdfQuadToPage: (quad) => applyQuad(toPage, quad as QuadIn<'pdf'>), pageQuadToPdf: (quad) => applyQuad(toPdf, quad as QuadIn<'content'>), } ; }
pageTransform [FunctionDeclaration]: export function pageTransform(input:{ pageSize: Size; rotation: PageRotation; scale: number; baseScale?: number; dpr: number; } ): PageTransform{ const{ pageSize, rotation, scale, dpr} = input; const deviceWidth = Math.max(1, Math.round(pageSize.width * scale * dpr)); const deviceHeight = deviceHeightForWidth(pageSize, deviceWidth); const renderScale = deviceWidth / pageSize.width; const content: Size ={ width: deviceWidth / dpr, height: deviceHeight / dpr} ; const viewScale = content.width / pageSize.width; const baseScale = input.baseScale ?? scale; const zoom = baseScale > 0 ? viewScale / baseScale : 1; const footprint = displaySize(content, rotation); const contentMat = [viewScale, 0, 0, viewScale, 0, 0] as Mat2D; const viewMat = rotateScaleMatrix(viewScale, content.width, content.height, rotation); const invMat = invert(viewMat); const toPixels = (point: Point): Point => applyPoint(contentMat, point); const contentInv = invert(contentMat); const fromPixels = (point: Point): Point => applyPoint(contentInv, point); const contentToView = (point: Point): Point => applyPoint(viewMat, point); const viewToContent = (point: Point): Point => applyPoint(invMat, point); const contentToViewRect = (rect: Rect): Rect => applyRect(viewMat, rect); const viewToContentRect = (rect: Rect): Rect => applyRect(invMat, rect); const cssMatrix = matrixToCss(viewMat); return{ rotation, viewWidth: footprint.width, viewHeight: footprint.height, contentWidth: content.width, contentHeight: content.height, deviceWidth, deviceHeight, renderScale, viewScale, baseScale, zoom, toPixels, fromPixels, contentToView, contentToViewRect, viewToContent, viewToContentRect, cssMatrix, } ; }
pdfToContentMatrix [FunctionDeclaration]: export function pdfToContentMatrix(crop:{ left: number; top: number} ): Mat2D<'pdf', 'content'>{ return [1, 0, 0, -1, -crop.left, crop.top] as Mat2D<'pdf', 'content'>; }
positionalQuad [FunctionDeclaration]: export function positionalQuad(quad: TextQuad): Quad{ return{ p1: quad.upperStart, p2: quad.upperEnd, p3: quad.lowerStart, p4: quad.lowerEnd} ; }
rectContains [FunctionDeclaration]: export function rectContains(rect: Rect, point: Point): boolean{ return ( point.x >= rect.x && point.x <= rect.x + rect.width && point.y >= rect.y && point.y <= rect.y + rect.height ); }
rectFromCorners [FunctionDeclaration]: export function rectFromCorners(from: Point, to: Point): Rect{ return{ x: Math.min(from.x, to.x), y: Math.min(from.y, to.y), width: Math.abs(to.x - from.x), height: Math.abs(to.y - from.y), } ; }
rectsOverlap [FunctionDeclaration]: export function rectsOverlap(left: Rect, right: Rect): boolean{ const overlap = intersectRects(left, right); return overlap.width > 0 && overlap.height > 0; }
rotate [FunctionDeclaration]: export function rotate<S extends Space>(rad: number): Mat2D<S, S>{ const cosine = Math.cos(rad); const sine = Math.sin(rad); return [cosine, sine, -sine, cosine, 0, 0] as Mat2D<S, S>; }
rotateAbout [FunctionDeclaration]: export function rotateAbout<S extends Space>(point: PointIn<S>, rad: number): Mat2D<S, S>{ return compose( translate<S>(point.x, point.y), compose(rotate<S>(rad), translate<S>(-point.x, -point.y)), ); }
rotateScaleMatrix [FunctionDeclaration]: export function rotateScaleMatrix( scale: number, boxW: number, boxH: number, rotation: PageRotation, ): Mat2D{ switch (rotation){ case 90: return [0, scale, -scale, 0, boxH, 0] as Mat2D; case 180: return [-scale, 0, 0, -scale, boxW, boxH] as Mat2D; case 270: return [0, -scale, scale, 0, 0, boxW] as Mat2D; default: return [scale, 0, 0, scale, 0, 0] as Mat2D; } }
scale [FunctionDeclaration]: export function scale<S extends Space>(sx: number, sy: number): Mat2D<S, S>{ return [sx, 0, 0, sy, 0, 0] as Mat2D<S, S>; }
scaleAbout [FunctionDeclaration]: export function scaleAbout<S extends Space>( point: PointIn<S>, sx: number, sy: number, ): Mat2D<S, S>{ return compose( translate<S>(point.x, point.y), compose(scale<S>(sx, sy), translate<S>(-point.x, -point.y)), ); }
snapToDevice [FunctionDeclaration]: export function snapToDevice(value: number, dpr: number): number{ return Math.round(value * dpr) / dpr; }
textQuadBounds [FunctionDeclaration]: export function textQuadBounds(quad: TextQuad): Rect{ const xs = [quad.upperStart.x, quad.upperEnd.x, quad.lowerStart.x, quad.lowerEnd.x]; const ys = [quad.upperStart.y, quad.upperEnd.y, quad.lowerStart.y, quad.lowerEnd.y]; const minX = Math.min(...xs); const minY = Math.min(...ys); return{ x: minX, y: minY, width: Math.max(...xs) - minX, height: Math.max(...ys) - minY} ; }
textQuadEdge [FunctionDeclaration]: export function textQuadEdge(quad: TextQuad, side: 'start' | 'end'): [Point, Point]{ return side === 'start' ? [quad.upperStart, quad.lowerStart] : [quad.upperEnd, quad.lowerEnd]; }
textQuadEquals [FunctionDeclaration]: export function textQuadEquals(left: TextQuad, right: TextQuad): boolean{ const eq = (point: Point, other: Point) => point.x === other.x && point.y === other.y; return ( eq(left.upperStart, right.upperStart) && eq(left.upperEnd, right.upperEnd) && eq(left.lowerStart, right.lowerStart) && eq(left.lowerEnd, right.lowerEnd) ); }
textQuadFromRect [FunctionDeclaration]: export function textQuadFromRect(rect: Rect): TextQuad{ return{ upperStart:{ x: rect.x, y: rect.y} , upperEnd:{ x: rect.x + rect.width, y: rect.y} , lowerStart:{ x: rect.x, y: rect.y + rect.height} , lowerEnd:{ x: rect.x + rect.width, y: rect.y + rect.height} , } ; }
textQuadPoints [FunctionDeclaration]: export function textQuadPoints(quad: TextQuad): [Point, Point, Point, Point]{ return [quad.upperStart, quad.upperEnd, quad.lowerStart, quad.lowerEnd]; }
textQuadRing [FunctionDeclaration]: export function textQuadRing(quad: TextQuad): [Point, Point, Point, Point]{ return [quad.upperStart, quad.upperEnd, quad.lowerEnd, quad.lowerStart]; }
translate [FunctionDeclaration]: export function translate<S extends Space>(tx: number, ty: number): Mat2D<S, S>{ return [1, 0, 0, 1, tx, ty] as Mat2D<S, S>; }
