# @embedpdf/core-geometry — .

Mat2D [TypeAliasDeclaration]: export type Mat2D<From extends Space = Space, To extends Space = Space> = readonly [ number, number, number, number, number, number, ] &{ readonly __from?: From; readonly __to?: To; } ;
NO_FRAME [VariableDeclaration]: NO_FRAME: PageFrame ={ top: 0, right: 0, bottom: 0, left: 0}
PageFrame [InterfaceDeclaration]: export interface PageFrame{ top: number; right: number; bottom: number; left: number; }
PageGeometry [InterfaceDeclaration]: export interface PageGeometry{ readonly pdfToContent: Mat2D<'pdf', 'content'>; readonly contentToView: Mat2D<'content', 'view'>; readonly pdfToView: Mat2D<'pdf', 'view'>; readonly viewToPdf: Mat2D<'view', 'pdf'>; }
PageGeometryInput [InterfaceDeclaration]: export interface PageGeometryInput{ crop:{ left: number; bottom: number; right: number; top: number} ; rotation: PageRotation; userUnit: number; }
PagePoint [TypeAliasDeclaration] (from packages/core/geometry/src/page-space.ts): export type PagePoint = PointIn<'content'>;
PageQuad [TypeAliasDeclaration] (from packages/core/geometry/src/page-space.ts): export type PageQuad = QuadIn<'content'>;
PageRect [TypeAliasDeclaration] (from packages/core/geometry/src/page-space.ts): export type PageRect = RectIn<'content'>;
PageRotation [TypeAliasDeclaration]: export type PageRotation = 0 | 90 | 180 | 270;
PageSpace [InterfaceDeclaration] (from packages/core/geometry/src/page-space.ts): export interface PageSpace{ readonly crop: PdfEdges; readonly width: number; readonly height: number; pdfToPage(point:{ x: number; y: number} ): PagePoint; pageToPdf(point:{ x: number; y: number} ): PointIn<'pdf'>; pdfRectToPage(rect: PdfEdges): PageRect; pageRectToPdf(rect: Rect): PdfEdges; pdfQuadToPage(quad: Quad): PageQuad; pageQuadToPdf(quad: Quad): QuadIn<'pdf'>; }
PageTransform [InterfaceDeclaration]: export interface PageTransform{ readonly rotation: PageRotation; readonly viewWidth: number; readonly viewHeight: number; readonly contentWidth: number; readonly contentHeight: number; readonly deviceWidth: number; readonly deviceHeight: number; readonly renderScale: number; readonly viewScale: number; readonly baseScale: number; readonly zoom: number; toPixels(p: Point): Point; fromPixels(p: Point): Point; contentToView(p: Point): Point; contentToViewRect(r: Rect): Rect; viewToContent(p: Point): Point; viewToContentRect(r: Rect): Rect; readonly cssMatrix: string; }
PdfEdges [InterfaceDeclaration] (from packages/core/geometry/src/page-space.ts): export interface PdfEdges{ readonly left: number; readonly bottom: number; readonly right: number; readonly top: number; }
Point [InterfaceDeclaration]: export interface Point{ x: number; y: number; }
PointIn [TypeAliasDeclaration]: export type PointIn<S extends Space> = Point & SpaceBrand<S>;
Quad [InterfaceDeclaration]: export interface Quad{ p1: Point; p2: Point; p3: Point; p4: Point; }
QuadIn [TypeAliasDeclaration]: export type QuadIn<S extends Space> = Quad & SpaceBrand<S>;
Rect [InterfaceDeclaration]: export interface Rect{ x: number; y: number; width: number; height: number; }
RectIn [TypeAliasDeclaration]: export type RectIn<S extends Space> = Rect & SpaceBrand<S>;
Size [InterfaceDeclaration]: export interface Size{ width: number; height: number; }
Space [TypeAliasDeclaration]: export type Space = 'pdf' | 'content' | 'view' | 'screen';
TextQuad [InterfaceDeclaration]: export interface TextQuad{ upperStart: Point; upperEnd: Point; lowerStart: Point; lowerEnd: Point; }
TextQuadIn [TypeAliasDeclaration]: export type TextQuadIn<S extends Space> = TextQuad & SpaceBrand<S>;
addRotations [FunctionDeclaration]: export function addRotations(a: PageRotation, b: PageRotation): PageRotation{ return ((a + b) % 360) as PageRotation; }
angleOf [FunctionDeclaration]: export function angleOf(m: Mat2D): number{ return Math.atan2(m[1], m[0]); }
applyPoint [FunctionDeclaration]: export function applyPoint<F extends Space, T extends Space>( m: Mat2D<F, T>, p: PointIn<F>, ): PointIn<T>{ const [a, b, c, d, e, f] = m; return{ x: a * p.x + c * p.y + e, y: b * p.x + d * p.y + f} as PointIn<T>; }
applyQuad [FunctionDeclaration]: export function applyQuad<F extends Space, T extends Space>( m: Mat2D<F, T>, q: QuadIn<F>, ): QuadIn<T>{ return{ p1: applyPoint(m, q.p1 as PointIn<F>), p2: applyPoint(m, q.p2 as PointIn<F>), p3: applyPoint(m, q.p3 as PointIn<F>), p4: applyPoint(m, q.p4 as PointIn<F>), } as QuadIn<T>; }
applyRect [FunctionDeclaration]: export function applyRect<F extends Space, T extends Space>( m: Mat2D<F, T>, r: RectIn<F>, ): RectIn<T>{ const c0 = applyPoint(m, { x: r.x, y: r.y} as PointIn<F>); const c1 = applyPoint(m, { x: r.x + r.width, y: r.y} as PointIn<F>); const c2 = applyPoint(m, { x: r.x, y: r.y + r.height} as PointIn<F>); const c3 = applyPoint(m, { x: r.x + r.width, y: r.y + r.height} as PointIn<F>); const xs = [c0.x, c1.x, c2.x, c3.x]; const ys = [c0.y, c1.y, c2.y, c3.y]; const minX = Math.min(...xs); const minY = Math.min(...ys); return{ x: minX, y: minY, width: Math.max(...xs) - minX, height: Math.max(...ys) - minY, } as RectIn<T>; }
applyTextQuad [FunctionDeclaration]: export function applyTextQuad<F extends Space, T extends Space>( m: Mat2D<F, T>, t: TextQuadIn<F>, ): TextQuadIn<T>{ return{ upperStart: applyPoint(m, t.upperStart as PointIn<F>), upperEnd: applyPoint(m, t.upperEnd as PointIn<F>), lowerStart: applyPoint(m, t.lowerStart as PointIn<F>), lowerEnd: applyPoint(m, t.lowerEnd as PointIn<F>), } as TextQuadIn<T>; }
boundsOfRects [FunctionDeclaration]: export function boundsOfRects(rects: readonly Rect[]): Rect | null{ if (rects.length === 0) return null; let x1 = Infinity; let y1 = Infinity; let x2 = -Infinity; let y2 = -Infinity; for (const r of rects){ x1 = Math.min(x1, r.x); y1 = Math.min(y1, r.y); x2 = Math.max(x2, r.x + r.width); y2 = Math.max(y2, r.y + r.height); } return{ x: x1, y: y1, width: x2 - x1, height: y2 - y1} ; }
compose [FunctionDeclaration]: export function compose<A extends Space, B extends Space, C extends Space>( m: Mat2D<B, C>, n: Mat2D<A, B>, ): Mat2D<A, C>{ const [a, b, c, d, e, f] = m; const [a2, b2, c2, d2, e2, f2] = n; return [ a * a2 + c * b2, b * a2 + d * b2, a * c2 + c * d2, b * c2 + d * d2, a * e2 + c * f2 + e, b * e2 + d * f2 + f, ] as Mat2D<A, C>; }
deviceHeightForWidth [FunctionDeclaration]: export function deviceHeightForWidth(pageSize: Size, deviceWidth: number): number{ return Math.max(1, Math.round((deviceWidth * pageSize.height) / pageSize.width)); }
displaySize [FunctionDeclaration]: export function displaySize(content: Size, rotation: PageRotation): Size{ return isQuarterTurn(rotation) ?{ width: content.height, height: content.width} :{ width: content.width, height: content.height} ; }
edgesOfQuad [FunctionDeclaration] (from packages/core/geometry/src/page-space.ts): export function edgesOfQuad(quad: Quad): PdfEdges{ const xs = [quad.p1.x, quad.p2.x, quad.p3.x, quad.p4.x]; const ys = [quad.p1.y, quad.p2.y, quad.p3.y, quad.p4.y]; return{ left: Math.min(...xs), right: Math.max(...xs), bottom: Math.min(...ys), top: Math.max(...ys), } ; }
edgesOverlap [FunctionDeclaration] (from packages/core/geometry/src/page-space.ts): export function edgesOverlap(a: PdfEdges, b: PdfEdges): boolean{ return ( Math.min(a.right, b.right) > Math.max(a.left, b.left) && Math.min(a.top, b.top) > Math.max(a.bottom, b.bottom) ); }
identity [FunctionDeclaration]: export function identity<S extends Space>(): Mat2D<S, S>{ return [1, 0, 0, 1, 0, 0] as Mat2D<S, S>; }
intersectRects [FunctionDeclaration]: export function intersectRects(a: Rect, b: Rect): Rect{ const x = Math.max(a.x, b.x); const y = Math.max(a.y, b.y); return{ x, y, width: Math.max(0, Math.min(a.x + a.width, b.x + b.width) - x), height: Math.max(0, Math.min(a.y + a.height, b.y + b.height) - y), } ; }
invert [FunctionDeclaration]: export function invert<F extends Space, T extends Space>(m: Mat2D<F, T>): Mat2D<T, F>{ const [a, b, c, d, e, f] = m; const det = a * d - b * c; if (det === 0) throw new Error('Mat2D.invert: non-invertible matrix (det = 0)'); const ia = d / det; const ib = -b / det; const ic = -c / det; const id = a / det; return [ia, ib, ic, id, -(ia * e + ic * f), -(ib * e + id * f)] as Mat2D<T, F>; }
isQuarterTurn [FunctionDeclaration]: export function isQuarterTurn(rotation: PageRotation): boolean{ return rotation === 90 || rotation === 270; }
matrixToCss [FunctionDeclaration]: export function matrixToCss(m: Mat2D): string{ const [a, b, c, d, e, f] = m; return `matrix(${ a} , ${ b} , ${ c} , ${ d} , ${ e} , ${ f} )`; }
normalizeQuad [FunctionDeclaration]: export function normalizeQuad(q: Quad): TextQuad{ const zigzag: QuadCornerList = [q.p1, q.p2, q.p3, q.p4]; if (zigzagWellFormed(zigzag)){ return{ upperStart: q.p1, upperEnd: q.p2, lowerStart: q.p3, lowerEnd: q.p4} ; } const ring: QuadCornerList = [q.p1, q.p2, q.p4, q.p3]; if (zigzagWellFormed(ring)){ return{ upperStart: q.p1, upperEnd: q.p2, lowerStart: q.p4, lowerEnd: q.p3} ; } const pts = [q.p1, q.p2, q.p3, q.p4].map((p) => (finitePoint(p) ? p :{ x: 0, y: 0} )); const cx = (pts[0].x + pts[1].x + pts[2].x + pts[3].x) / 4; const cy = (pts[0].y + pts[1].y + pts[2].y + pts[3].y) / 4; const ringSorted = [...pts].sort( (a, b) => Math.atan2(a.y - cy, a.x - cx) - Math.atan2(b.y - cy, b.x - cx), ); let upperEdge = 0; let bestY = Infinity; for (let i = 0; i < 4; i++){ const midY = (ringSorted[i].y + ringSorted[(i + 1) % 4].y) / 2; if (midY < bestY){ bestY = midY; upperEdge = i; } } const a = ringSorted[upperEdge]; const b = ringSorted[(upperEdge + 1) % 4]; const c = ringSorted[(upperEdge + 2) % 4]; const d = ringSorted[(upperEdge + 3) % 4]; const startFirst = a.x <= b.x; return startFirst ?{ upperStart: a, upperEnd: b, lowerStart: d, lowerEnd: c} :{ upperStart: b, upperEnd: a, lowerStart: c, lowerEnd: d} ; }
pageGeometry [FunctionDeclaration]: export function pageGeometry(input: PageGeometryInput, zoom: number): PageGeometry{ const{ crop, rotation, userUnit} = input; const s = zoom * userUnit; const pdfToContent = pdfToContentMatrix(crop); const Wc = (crop.right - crop.left) * s; const Hc = (crop.top - crop.bottom) * s; const contentToView = rotateScaleMatrix(s, Wc, Hc, rotation) as Mat2D<'content', 'view'>; const pdfToView = compose(contentToView, pdfToContent); return{ pdfToContent, contentToView, pdfToView, viewToPdf: invert(pdfToView)} ; }
pageSpace [FunctionDeclaration] (from packages/core/geometry/src/page-space.ts): export function pageSpace(crop: PdfEdges): PageSpace{ const toPage: Mat2D<'pdf', 'content'> = pdfToContentMatrix(crop); const toPdf: Mat2D<'content', 'pdf'> = invert(toPage); return{ crop, width: crop.right - crop.left, height: crop.top - crop.bottom, pdfToPage: (point) => applyPoint(toPage, point as PointIn<'pdf'>), pageToPdf: (point) => applyPoint(toPdf, point as PointIn<'content'>), pdfRectToPage: (rect) => ({ x: rect.left - crop.left, y: crop.top - rect.top, width: rect.right - rect.left, height: rect.top - rect.bottom, } ) as PageRect, pageRectToPdf: (rect) => ({ left: rect.x + crop.left, right: rect.x + rect.width + crop.left, top: crop.top - rect.y, bottom: crop.top - (rect.y + rect.height), } ), pdfQuadToPage: (quad) => applyQuad(toPage, quad as QuadIn<'pdf'>), pageQuadToPdf: (quad) => applyQuad(toPdf, quad as QuadIn<'content'>), } ; }
pageTransform [FunctionDeclaration]: export function pageTransform(input:{ pageSize: Size; rotation: PageRotation; scale: number; baseScale?: number; dpr: number; } ): PageTransform{ const{ pageSize, rotation, scale, dpr} = input; const deviceWidth = Math.max(1, Math.round(pageSize.width * scale * dpr)); const deviceHeight = deviceHeightForWidth(pageSize, deviceWidth); const renderScale = deviceWidth / pageSize.width; const content: Size ={ width: deviceWidth / dpr, height: deviceHeight / dpr} ; const viewScale = content.width / pageSize.width; const baseScale = input.baseScale ?? scale; const zoom = baseScale > 0 ? viewScale / baseScale : 1; const footprint = displaySize(content, rotation); const contentMat = [viewScale, 0, 0, viewScale, 0, 0] as Mat2D; const viewMat = rotateScaleMatrix(viewScale, content.width, content.height, rotation); const invMat = invert(viewMat); const toPixels = (p: Point): Point => applyPoint(contentMat, p); const contentInv = invert(contentMat); const fromPixels = (p: Point): Point => applyPoint(contentInv, p); const contentToView = (p: Point): Point => applyPoint(viewMat, p); const viewToContent = (p: Point): Point => applyPoint(invMat, p); const contentToViewRect = (r: Rect): Rect => applyRect(viewMat, r); const viewToContentRect = (r: Rect): Rect => applyRect(invMat, r); const cssMatrix = matrixToCss(viewMat); return{ rotation, viewWidth: footprint.width, viewHeight: footprint.height, contentWidth: content.width, contentHeight: content.height, deviceWidth, deviceHeight, renderScale, viewScale, baseScale, zoom, toPixels, fromPixels, contentToView, contentToViewRect, viewToContent, viewToContentRect, cssMatrix, } ; }
pdfToContentMatrix [FunctionDeclaration]: export function pdfToContentMatrix(crop:{ left: number; top: number} ): Mat2D<'pdf', 'content'>{ return [1, 0, 0, -1, -crop.left, crop.top] as Mat2D<'pdf', 'content'>; }
positionalQuad [FunctionDeclaration]: export function positionalQuad(t: TextQuad): Quad{ return{ p1: t.upperStart, p2: t.upperEnd, p3: t.lowerStart, p4: t.lowerEnd} ; }
rectContains [FunctionDeclaration]: export function rectContains(rect: Rect, point: Point): boolean{ return ( point.x >= rect.x && point.x <= rect.x + rect.width && point.y >= rect.y && point.y <= rect.y + rect.height ); }
rectFromCorners [FunctionDeclaration]: export function rectFromCorners(a: Point, b: Point): Rect{ return{ x: Math.min(a.x, b.x), y: Math.min(a.y, b.y), width: Math.abs(b.x - a.x), height: Math.abs(b.y - a.y), } ; }
rectsOverlap [FunctionDeclaration]: export function rectsOverlap(a: Rect, b: Rect): boolean{ const i = intersectRects(a, b); return i.width > 0 && i.height > 0; }
rotate [FunctionDeclaration]: export function rotate<S extends Space>(rad: number): Mat2D<S, S>{ const c = Math.cos(rad); const s = Math.sin(rad); return [c, s, -s, c, 0, 0] as Mat2D<S, S>; }
rotateAbout [FunctionDeclaration]: export function rotateAbout<S extends Space>(c: PointIn<S>, rad: number): Mat2D<S, S>{ return compose(translate<S>(c.x, c.y), compose(rotate<S>(rad), translate<S>(-c.x, -c.y))); }
rotateScaleMatrix [FunctionDeclaration]: export function rotateScaleMatrix( scale: number, boxW: number, boxH: number, rotation: PageRotation, ): Mat2D{ switch (rotation){ case 90: return [0, scale, -scale, 0, boxH, 0] as Mat2D; case 180: return [-scale, 0, 0, -scale, boxW, boxH] as Mat2D; case 270: return [0, -scale, scale, 0, 0, boxW] as Mat2D; default: return [scale, 0, 0, scale, 0, 0] as Mat2D; } }
scale [FunctionDeclaration]: export function scale<S extends Space>(sx: number, sy: number): Mat2D<S, S>{ return [sx, 0, 0, sy, 0, 0] as Mat2D<S, S>; }
scaleAbout [FunctionDeclaration]: export function scaleAbout<S extends Space>(a: PointIn<S>, sx: number, sy: number): Mat2D<S, S>{ return compose(translate<S>(a.x, a.y), compose(scale<S>(sx, sy), translate<S>(-a.x, -a.y))); }
snapToDevice [FunctionDeclaration]: export function snapToDevice(value: number, dpr: number): number{ return Math.round(value * dpr) / dpr; }
textQuadBounds [FunctionDeclaration]: export function textQuadBounds(t: TextQuad): Rect{ const xs = [t.upperStart.x, t.upperEnd.x, t.lowerStart.x, t.lowerEnd.x]; const ys = [t.upperStart.y, t.upperEnd.y, t.lowerStart.y, t.lowerEnd.y]; const minX = Math.min(...xs); const minY = Math.min(...ys); return{ x: minX, y: minY, width: Math.max(...xs) - minX, height: Math.max(...ys) - minY} ; }
textQuadEdge [FunctionDeclaration]: export function textQuadEdge(t: TextQuad, side: 'start' | 'end'): [Point, Point]{ return side === 'start' ? [t.upperStart, t.lowerStart] : [t.upperEnd, t.lowerEnd]; }
textQuadEquals [FunctionDeclaration]: export function textQuadEquals(a: TextQuad, b: TextQuad): boolean{ const eq = (p: Point, q: Point) => p.x === q.x && p.y === q.y; return ( eq(a.upperStart, b.upperStart) && eq(a.upperEnd, b.upperEnd) && eq(a.lowerStart, b.lowerStart) && eq(a.lowerEnd, b.lowerEnd) ); }
textQuadFromRect [FunctionDeclaration]: export function textQuadFromRect(r: Rect): TextQuad{ return{ upperStart:{ x: r.x, y: r.y} , upperEnd:{ x: r.x + r.width, y: r.y} , lowerStart:{ x: r.x, y: r.y + r.height} , lowerEnd:{ x: r.x + r.width, y: r.y + r.height} , } ; }
textQuadPoints [FunctionDeclaration]: export function textQuadPoints(t: TextQuad): [Point, Point, Point, Point]{ return [t.upperStart, t.upperEnd, t.lowerStart, t.lowerEnd]; }
textQuadRing [FunctionDeclaration]: export function textQuadRing(t: TextQuad): [Point, Point, Point, Point]{ return [t.upperStart, t.upperEnd, t.lowerEnd, t.lowerStart]; }
translate [FunctionDeclaration]: export function translate<S extends Space>(tx: number, ty: number): Mat2D<S, S>{ return [1, 0, 0, 1, tx, ty] as Mat2D<S, S>; }
