# @embedpdf/react — ./selection

HANDLE_BAR [VariableDeclaration] (from packages/plugin/selection/src/handles.ts): HANDLE_BAR = 2
HANDLE_HEAD [VariableDeclaration] (from packages/plugin/selection/src/handles.ts): HANDLE_HEAD = 12
HANDLE_PAD [VariableDeclaration] (from packages/plugin/selection/src/handles.ts): HANDLE_PAD = 14
SelectionAnchor [InterfaceDeclaration] (from packages/plugin/selection/src/contract.ts): export interface SelectionAnchor{ page: PageRef; bounds: Rect; }
SelectionCapability [InterfaceDeclaration] (from packages/plugin/selection/src/contract.ts): export interface SelectionCapability{ canSelect(): boolean; canCopy(): boolean; select(range: SelectionRangeInput): void; selectAll(): void; selectPage(page: PageRef): void; selectWordAt(page: PageRef, point: Point): boolean; selectLineAt(page: PageRef, point: Point): boolean; extendTo(page: PageRef, point: Point): void; clear(): void; hasSelection(): boolean; getSnapshot(): SelectionSnapshot; getRange(): TextRange | null; listSelectedPages(): readonly PageRef[]; listSegments(page: PageRef): readonly SelectionSegment[]; listRects(page: PageRef): readonly Rect[]; getAnchor(): SelectionAnchor | null; readText(options?: OperationOptions): Promise<string>; readTextInRange(range: TextRange, options?: OperationOptions): Promise<string>; readonly onChanged: EventHook<SelectionChangedEvent>; readonly onCommitted: EventHook<SelectionCommittedEvent>; readonly onCleared: EventHook<SelectionClearedEvent>; }
SelectionChangedEvent [InterfaceDeclaration] (from packages/plugin/selection/src/contract.ts): export interface SelectionChangedEvent{ readonly range: TextRange | null; readonly pages: readonly PageRef[]; }
SelectionClearedEvent [TypeAliasDeclaration] (from packages/plugin/selection/src/contract.ts): export type SelectionClearedEvent = Record<string, never>;
SelectionClipboard [FunctionDeclaration]: export function SelectionClipboard({ prefetch} : SelectionClipboardProps ={ } ){ const selection = useCapability(SelectionToken); useEffect( () => wireSelectionClipboard(selection, prefetch === undefined ?{ } :{ prefetch} ), [selection, prefetch], ); return null; }
SelectionClipboardProps [TypeAliasDeclaration]: export type SelectionClipboardProps = Pick<SelectionClipboardOptions, 'prefetch'>;
SelectionCommittedEvent [InterfaceDeclaration] (from packages/plugin/selection/src/contract.ts): export interface SelectionCommittedEvent{ readonly range: TextRange; }
SelectionConfig [InterfaceDeclaration] (from packages/plugin/selection/src/contract.ts): export interface SelectionConfig{ dragThreshold?: number; }
SelectionEndpoint [InterfaceDeclaration] (from packages/plugin/selection/src/contract.ts): export interface SelectionEndpoint{ page: PageRef; glyphQuad: TextQuad; advance: 1 | -1; rect: Rect; }
SelectionHandleDragSession [InterfaceDeclaration] (from packages/plugin/selection/src/handles.ts): export interface SelectionHandleDragSession{ move(overlay: Point): void; end(): void; }
SelectionHandleEndpoint [InterfaceDeclaration] (from packages/plugin/selection/src/handles.ts): export interface SelectionHandleEndpoint{ page: PageRef; glyphQuad: TextQuad; advance: 1 | -1; }
SelectionHandleGeom [InterfaceDeclaration] (from packages/plugin/selection/src/handles.ts): export interface SelectionHandleGeom{ bar:{ from: Point; to: Point} ; length: number; rotation: number; upright: boolean; head: Point; }
SelectionHandleTarget [InterfaceDeclaration] (from packages/plugin/selection/src/handles.ts): export interface SelectionHandleTarget{ beginGestureAt(page: PageRef, point: Point): boolean; extendTo(page: PageRef, point: Point): void; endGesture(): void; }
SelectionHandleView [InterfaceDeclaration] (from packages/plugin/selection/src/handles.ts): export interface SelectionHandleView{ toOverlay(page: PageRef, point: Point): Point | null; pageAt(overlay: Point):{ ref: PageRef; point: Point} | null; pointOnPage(page: PageRef, overlay: Point): Point | null; }
SelectionHandles [FunctionDeclaration]: export function SelectionHandles({ color = '#2196f3', token: explicitToken, } : SelectionHandlesProps){ const token = useStageToken(explicitToken); const host = useCapability(SelectionHostToken); const stage = useOptionalCapability(token); const surface = useOptionalProjectorBinding(); if (!surface || surface.projector.space !== 'overlay'){ devWarn( 'selection-handles-outside-stage', '<SelectionHandles> renders nothing here: mount it in the <Stage> overlay slot ' + '(a <PageView> has no camera to project the handles through).', ); } const selecting = useSelector(SelectionHostToken, (selection) => selection.isGestureActive()); const visible = useSelector(SelectionHostToken, (selection) => selection.isHighlightVisible()); const endpoints = useSelector( SelectionHostToken, (selection): Endpoints | null =>{ const snapshot = selection.getSnapshot(); if (!snapshot.start || !snapshot.end) return null; return{ start:{ page: snapshot.start.page, glyphQuad: snapshot.start.glyphQuad, advance: snapshot.start.advance, } , end:{ page: snapshot.end.page, glyphQuad: snapshot.end.glyphQuad, advance: snapshot.end.advance, } , } ; } , sameEndpoints, ); useKernelValue(() => stage?.listVisiblePages() ?? null); const [dragging, setDragging] = useState<'start' | 'end' | null>(null); const armSource = useRef<{ stage: StageCapability; endpoints: Endpoints} | null>(null); armSource.current = stage && endpoints ?{ stage, endpoints} : null; const bindHandle = useMemo(() =>{ const detach: Partial<Record<'start' | 'end', () => void>> ={ } ; const make = (role: 'start' | 'end') => (element: HTMLDivElement | null) =>{ detach[role]?.(); delete detach[role]; if (!element) return; detach[role] = attachSelectionHandle(element, { arm: () =>{ const src = armSource.current; if (!src) return null; const view = handleView(src.stage); const geometry = selectionHandleGeom(view, src.endpoints[role], role); if (!geometry) return null; const opposite = src.endpoints[role === 'start' ? 'end' : 'start']; const drag = createSelectionHandleDrag(host, view, opposite, src.endpoints[role].page); setDragging(role); return{ base:{ x: (geometry.bar.from.x + geometry.bar.to.x) / 2, y: (geometry.bar.from.y + geometry.bar.to.y) / 2, } , session:{ move: drag.move, end: () =>{ drag.end(); setDragging(null); } , } , } ; } , } ); } ; return{ start: make('start'), end: make('end')} ; } , [host]); if (!stage || !endpoints || !visible) return null; if (selecting && !dragging) return null; const view = handleView(stage); const renderHandle = (role: 'start' | 'end') =>{ const geometry = selectionHandleGeom(view, endpoints[role], role); if (!geometry) return null; const barTop = HANDLE_PAD + (role === 'start' ? HANDLE_HEAD : 0); const pivotX = HANDLE_PAD + HANDLE_BAR / 2; return ( <div key={ role} ref={ bindHandle[role]} style={ { position: 'absolute', left: geometry.bar.from.x - pivotX, top: geometry.bar.from.y - barTop, width: HANDLE_BAR + 2 * HANDLE_PAD, height: geometry.length + HANDLE_HEAD + 2 * HANDLE_PAD, ...(geometry.upright ? null :{ transform: `rotate(${ geometry.rotation} deg)`, transformOrigin: `${ pivotX} px ${ barTop} px`, } ), touchAction: 'none', cursor: 'grab', pointerEvents: 'auto', } } >{ } <div style={ { position: 'absolute', left: HANDLE_PAD, top: barTop, width: HANDLE_BAR, height: geometry.length, background: color, borderRadius: HANDLE_BAR / 2, } } />{ } <div style={ { position: 'absolute', left: pivotX - HANDLE_HEAD / 2, top: role === 'start' ? HANDLE_PAD : HANDLE_PAD + geometry.length, width: HANDLE_HEAD, height: HANDLE_HEAD, borderRadius: '50%', background: color, boxShadow: '0 1px 4px rgba(0, 0, 0, 0.35)', } } /> </div> ); } ; return ( <>{ renderHandle('start')} { renderHandle('end')} </> ); }
SelectionHandlesProps [InterfaceDeclaration]: export interface SelectionHandlesProps{ color?: string; token?: CapabilityToken<StageCapability>; }
SelectionLayer [FunctionDeclaration]: export function SelectionLayer({ color = 'rgba(33, 150, 243, 0.35)'} : SelectionLayerProps){ const page = usePage(); const selection = useCapability(SelectionHostToken); const segments = useSelector( SelectionHostToken, (selection) => selection.listSegments(page.ref), shallowArray, ); const visible = useSelector(SelectionHostToken, (selection) => selection.isHighlightVisible()); useEffect(() =>{ void selection.ensureLoaded(page.ref); } , [selection, page.ref]); if (!visible) return null; return ( <svg style={ { position: 'absolute', inset: 0, width: '100%', height: '100%', overflow: 'visible', pointerEvents: 'none', } } >{ segments.map((segment, i) =>{ const ring = [ segment.quad.upperStart, segment.quad.upperEnd, segment.quad.lowerEnd, segment.quad.lowerStart, ].map((point) => page.transform.toPixels(point)); return ( <polygon key={ i} points={ ring.map((point) => `${ point.x} , ${ point.y} `).join(' ')} fill={ color} /> ); } )} </svg> ); }
SelectionLayerProps [InterfaceDeclaration]: export interface SelectionLayerProps{ color?: string; }
SelectionMenu [FunctionDeclaration]: export function SelectionMenu({ children, gap = 8, placement = 'top'} : SelectionMenuProps){ const selecting = useSelector(SelectionHostToken, (selection) => selection.isGestureActive()); const anchor = useSelector(SelectionToken, (selection) => selection.getAnchor(), sameAnchor); if (selecting || !anchor) return null; return ( <Anchored anchor={ anchor} placement={ placement} gap={ gap} >{ children} </Anchored> ); }
SelectionMenuProps [InterfaceDeclaration]: export interface SelectionMenuProps{ children: React.ReactNode; gap?: number; placement?: AnchoredPlacement; }
SelectionRangeInput [TypeAliasDeclaration] (from packages/plugin/selection/src/contract.ts): export type SelectionRangeInput = TextRange |{ page: PageRef; start: number; count: number} ;
SelectionSegment [InterfaceDeclaration] (from packages/plugin/selection/src/geometry.ts): export interface SelectionSegment{ quad: TextQuad; rect: Rect; advance: 1 | -1; }
SelectionSnapshot [InterfaceDeclaration] (from packages/plugin/selection/src/contract.ts): export interface SelectionSnapshot{ readonly pages: readonly{ page: PageRef; segments: readonly SelectionSegment[]} []; readonly start: SelectionEndpoint | null; readonly end: SelectionEndpoint | null; readonly direction: 'forward' | 'backward'; readonly range: TextRange | null; }
SelectionToken [VariableDeclaration] (from packages/plugin/selection/src/token.ts): SelectionToken = createCapabilityToken<SelectionCapability>('selection', { hint: `add selectionPlugin() from '@embedpdf/plugin-selection' to your plugins list`, } )
TextPosition [InterfaceDeclaration] (from packages/plugin/selection/src/contract.ts): export interface TextPosition{ page: PageRef; index: number; }
TextRange [InterfaceDeclaration] (from packages/plugin/selection/src/contract.ts): export interface TextRange{ start: TextPosition; end: TextPosition; }
copySelection [FunctionDeclaration] (from packages/framework/web/src/clipboard.ts): export async function copySelection(selection: ClipboardSelectionSource): Promise<string>{ const text = await selection.readText(); if (text !== '') await navigator.clipboard.writeText(text); return text; }
createSelectionHandleDrag [FunctionDeclaration] (from packages/plugin/selection/src/handles.ts): export function createSelectionHandleDrag( selection: SelectionHandleTarget, view: SelectionHandleView, opposite: SelectionHandleEndpoint, draggedPage: PageRef, ): SelectionHandleDragSession{ const quad = opposite.glyphQuad; const anchorPoint = midpoint( midpoint(quad.upperStart, quad.lowerEnd), midpoint(quad.upperEnd, quad.lowerStart), ); let begun = false; let lastPage = draggedPage; return{ move: (overlay) =>{ if (!begun){ if (!selection.beginGestureAt(opposite.page, anchorPoint)) return; begun = true; } const hit = view.pageAt(overlay); if (hit){ lastPage = hit.ref; selection.extendTo(hit.ref, hit.point); } else{ const point = view.pointOnPage(lastPage, overlay); if (point) selection.extendTo(lastPage, point); } } , end: () =>{ if (begun) selection.endGesture(); } , } ; }
selectionHandleGeom [FunctionDeclaration] (from packages/plugin/selection/src/handles.ts): export function selectionHandleGeom( view: SelectionHandleView, endpoint: SelectionHandleEndpoint, role: 'start' | 'end', ): SelectionHandleGeom | null{ const leading = role === 'start' ? endpoint.advance > 0 : endpoint.advance < 0; const [upperPage, lowerPage] = textQuadEdge(endpoint.glyphQuad, leading ? 'start' : 'end'); const upper = view.toOverlay(endpoint.page, upperPage); const lower = view.toOverlay(endpoint.page, lowerPage); if (!upper || !lower) return null; const dx = lower.x - upper.x; const dy = lower.y - upper.y; const length = Math.hypot(dx, dy); if (length === 0) return null; const rotation = (Math.atan2(dy, dx) * 180) / Math.PI - 90; const upright = Math.abs(rotation) < HANDLE_ROT_EPSILON; const ux = dx / length; const uy = dy / length; const head = role === 'start' ?{ x: upper.x - (ux * HANDLE_HEAD) / 2, y: upper.y - (uy * HANDLE_HEAD) / 2} :{ x: lower.x + (ux * HANDLE_HEAD) / 2, y: lower.y + (uy * HANDLE_HEAD) / 2} ; return{ bar:{ from: upper, to: lower} , length, rotation, upright, head} ; }
selectionPlugin [VariableDeclaration] (from packages/plugin/selection/src/selection.plugin.ts): selectionPlugin = (config: SelectionConfig ={ } ) => definePlugin<SelectionState, SelectionHostCapability>({ id: 'selection', token: SelectionToken, scope: 'document', requires: [InteractionToken], optional: [FeedbackToken], state: initialSelectionState, create: (ctx) => createSelectionController(ctx, config), } )
useSelection [FunctionDeclaration]: export function useSelection(){ return useCapability(SelectionToken); }
useSelectionEvent [FunctionDeclaration]: export function useSelectionEvent<T>( select: (selection: SelectionCapability) => EventHook<T>, handler: (event: T) => void, ): void{ useCapabilityEvent(SelectionToken, select, handler); }
useSelectionState [FunctionDeclaration]: export function useSelectionState():{ hasSelection: boolean; range: TextRange | null; pageRefs: readonly PageRef[]; } { const hasSelection = useSelector(SelectionToken, (selection) => selection.hasSelection()); const range = useSelector(SelectionToken, (selection) => selection.getRange(), sameRange); const pageRefs = useSelector( SelectionToken, (selection) => selection.listSelectedPages(), samePages, ); return{ hasSelection, range, pageRefs} ; }
