# @embedpdf/react — ./toolbar

AddItemSpec [InterfaceDeclaration] (from packages/core/ui/src/transforms.ts): export interface AddItemSpec{ readonly bar: string; readonly section: keyof BarSections; readonly group: string; readonly item: BarChild; readonly at?: number; }
BarChild [TypeAliasDeclaration] (from packages/core/ui/src/schema.ts): export type BarChild = BarItem | CustomItem | string;
BarGroup [InterfaceDeclaration] (from packages/core/ui/src/schema.ts): export interface BarGroup{ readonly id: string; readonly labelKey?: string; readonly role?: 'buttons' | 'tabs'; readonly shed?: boolean; readonly collapse?: 'menu' | 'select'; readonly importance?: Importance; readonly items: readonly BarChild[]; }
BarItem [InterfaceDeclaration] (from packages/core/ui/src/schema.ts): export interface BarItem{ readonly command: string; readonly variants?: readonly Variant[]; readonly importance?: Importance; }
BarSchema [InterfaceDeclaration] (from packages/core/ui/src/schema.ts): export interface BarSchema{ readonly id: string; readonly sections: BarSections; }
BarSections [InterfaceDeclaration] (from packages/core/ui/src/schema.ts): export interface BarSections{ readonly start?: readonly BarGroup[]; readonly center?: readonly BarGroup[]; readonly end?: readonly BarGroup[]; }
ChromeHelpers [TypeAliasDeclaration] (from packages/core/ui/src/helpers.ts): export type ChromeHelpers = typeof chromeHelpers;
ChromeSchema [InterfaceDeclaration] (from packages/core/ui/src/schema.ts): export interface ChromeSchema{ readonly bars: Readonly<Record<string, BarSchema>>; readonly modeBars?: Readonly<Record<string, BarSchema>>; readonly menus?: Readonly<Record<string, MenuSchema>>; readonly strips?: Readonly<Record<string, BarSchema>>; readonly frame?: FrameSchema; }
CollapsedGroupView [InterfaceDeclaration]: export interface CollapsedGroupView{ readonly id: string; readonly labelKey?: string; readonly collapse: 'menu' | 'select'; readonly role: 'buttons' | 'tabs'; readonly commands: readonly ResolvedCommand[]; execute(id: string): void; }
CustomItem [InterfaceDeclaration] (from packages/core/ui/src/schema.ts): export interface CustomItem{ readonly slot: string; readonly variants?: readonly string[]; readonly terminal: string; readonly importance?: Importance; }
CustomSlotCtx [InterfaceDeclaration]: export interface CustomSlotCtx{ layer: 'live' | 'measure'; measure: (width: number) => void; }
DEFAULT_IMPORTANCE [VariableDeclaration] (from packages/core/ui/src/schema.ts): DEFAULT_IMPORTANCE: Importance = 3
FitMetrics [InterfaceDeclaration] (from packages/core/ui/src/solver.ts): export interface FitMetrics{ unit(key: string, variant: string): number | undefined; groupCollapsed(groupId: string): number | undefined; groupTrigger(groupId: string): number | undefined; readonly overflowTrigger: number; readonly gap: number; readonly separator: number; }
FitResult [InterfaceDeclaration] (from packages/core/ui/src/solver.ts): export interface FitResult{ readonly units: ReadonlyMap<string, UnitAssignment>; readonly groups: ReadonlyMap<string, GroupAssignment>; readonly hasOverflow: boolean; readonly width: number; readonly complete: boolean; }
FrameSchema [InterfaceDeclaration] (from packages/core/ui/src/schema.ts): export interface FrameSchema{ readonly toolbar?: 'top' | 'bottom'; readonly tabs?: 'always' | 'multiple' | 'never'; readonly header?: boolean; }
GroupAssignment [InterfaceDeclaration] (from packages/core/ui/src/solver.ts): export interface GroupAssignment{ readonly shedCount: number; readonly collapsed: boolean; readonly overflowed: boolean; }
GroupDisclosureView [InterfaceDeclaration]: export interface GroupDisclosureView{ readonly id: string; readonly labelKey?: string; readonly role: 'buttons' | 'tabs'; readonly commands: readonly ResolvedCommand[]; execute(id: string): void; }
Importance [TypeAliasDeclaration] (from packages/core/ui/src/schema.ts): export type Importance = 1 | 2 | 3 | 4 | 5;
KeyStroke [InterfaceDeclaration] (from packages/core/ui/src/shortcuts.ts): export interface KeyStroke{ readonly key: string; readonly code?: string; readonly ctrlKey: boolean; readonly metaKey: boolean; readonly altKey: boolean; readonly shiftKey: boolean; }
MenuSchema [InterfaceDeclaration] (from packages/core/ui/src/schema.ts): export interface MenuSchema{ readonly id: string; readonly sections: readonly MenuSection[]; }
MenuSection [InterfaceDeclaration] (from packages/core/ui/src/schema.ts): export interface MenuSection{ readonly labelKey?: string; readonly items: readonly string[]; }
NormalizedBar [InterfaceDeclaration] (from packages/core/ui/src/schema.ts): export interface NormalizedBar{ readonly id: string; readonly sections: readonly NormalizedSection[]; }
NormalizedGroup [InterfaceDeclaration] (from packages/core/ui/src/schema.ts): export interface NormalizedGroup{ readonly id: string; readonly labelKey?: string; readonly role: 'buttons' | 'tabs'; readonly shed: boolean; readonly collapse?: 'menu' | 'select'; readonly importance: Importance; readonly units: readonly NormalizedUnit[]; }
NormalizedSection [InterfaceDeclaration] (from packages/core/ui/src/schema.ts): export interface NormalizedSection{ readonly name: 'start' | 'center' | 'end'; readonly groups: readonly NormalizedGroup[]; }
NormalizedUnit [TypeAliasDeclaration] (from packages/core/ui/src/schema.ts): export type NormalizedUnit = |{ readonly kind: 'command'; readonly key: string; readonly command: string; readonly variants: readonly string[]; readonly importance: Importance; } |{ readonly kind: 'custom'; readonly key: string; readonly slot: string; readonly variants: readonly string[]; readonly terminal: string; readonly importance: Importance; } ;
OverflowMenuView [InterfaceDeclaration]: export interface OverflowMenuView{ readonly sections: readonly OverflowSection[]; readonly isOpen: boolean; close(): void; resolve(id: string): ResolvedCommand | null; execute(id: string): void; }
OverflowRow [TypeAliasDeclaration] (from packages/core/ui/src/projection.ts): export type OverflowRow = |{ readonly type: 'command'; readonly command: string} |{ readonly type: 'submenu'; readonly command: string; readonly menu: string} ;
OverflowSection [InterfaceDeclaration] (from packages/core/ui/src/projection.ts): export interface OverflowSection{ readonly labelKey?: string; readonly role?: 'radio'; readonly rows: readonly OverflowRow[]; }
PINNED [VariableDeclaration] (from packages/core/ui/src/schema.ts): PINNED: Importance = 5
ParsedShortcut [InterfaceDeclaration] (from packages/core/ui/src/shortcuts.ts): export interface ParsedShortcut{ readonly key: string; readonly mod: boolean; readonly ctrl: boolean; readonly meta: boolean; readonly alt: boolean; readonly shift: boolean; }
ResolveMenuTarget [TypeAliasDeclaration] (from packages/core/ui/src/projection.ts): export type ResolveMenuTarget = (commandId: string) =>{ menu?: string} | null;
StripGroup [InterfaceDeclaration] (from packages/core/ui/src/projection.ts): export interface StripGroup{ readonly id: string; readonly labelKey?: string; readonly commands: readonly string[]; }
StripView [InterfaceDeclaration]: export interface StripView{ readonly groups: readonly StripViewGroup[]; execute(id: string): void; }
StripViewGroup [InterfaceDeclaration]: export interface StripViewGroup{ readonly id: string; readonly labelKey?: string; readonly commands: readonly ResolvedCommand[]; }
Toolbar [FunctionDeclaration]: export function Toolbar({ bar, gap = 8, separatorWidth = 1, className, style, renderCommand = defaultRenderCommand, renderCustom, renderCollapsed, renderGroupTrigger, renderSeparator = defaultRenderSeparator, renderOverflowTrigger = defaultRenderOverflowTrigger, renderOverflowMenu, } : ToolbarProps){ const kernel = useKernel(); const commands = useCapability(CommandsToken); const documentId = useDocumentId(); const [, force] = useReducer((x: number) => x + 1, 0); useEffect(() => kernel.subscribe(force), [kernel]); const containerRef = useRef<HTMLDivElement>(null); const [containerWidth, setContainerWidth] = useState(0); useLayoutEffect(() =>{ const element = containerRef.current; if (!element) return; const observer = new ResizeObserver((entries) =>{ const width = entries[entries.length - 1]?.contentRect.width; if (width !== undefined) setContainerWidth((previous) => (Math.abs(previous - width) > 0.5 ? width : previous)); } ); observer.observe(element); setContainerWidth(element.clientWidth); return () => observer.disconnect(); } , []); const widthsRef = useRef(new Map<string, number>()); const [, bumpMeasureVersion] = useReducer((x: number) => x + 1, 0); const onWidth = useMemo( () => (key: string, width: number) =>{ const previous = widthsRef.current.get(key); if (previous !== undefined && Math.abs(previous - width) <= 0.5) return; widthsRef.current.set(key, width); bumpMeasureVersion(); } , [], ); const resolveCmd = (id: string): ResolvedCommand | null => commands.resolveCommand(id, documentId ?? undefined); const executeCmd = (id: string) => void commands.execute(id, { documentId: documentId ?? undefined} ); const commandOf = (unit: NormalizedUnit) => unit.kind === 'command' ? unit.command : unit.terminal; const normalized = useMemo(() => normalizeBar(bar), [bar]); const visibleBar = filterBar(normalized, (unit) => resolveCmd(commandOf(unit))?.visible === true); const metrics: FitMetrics ={ unit: (key, variant) => widthsRef.current.get(`u:${ key} @${ variant} `), groupCollapsed: (id) => widthsRef.current.get(groupKey(id)), groupTrigger: (id) => widthsRef.current.get(groupTriggerKey(id)), overflowTrigger: widthsRef.current.get(TRIGGER_KEY) ?? 32, gap, separator: separatorWidth + gap, } ; const fit = solve(visibleBar, metrics, containerWidth); const overflowSections = projectOverflow(visibleBar, fit, (id) => commands.getMenuTarget(id)); const [overflowOpen, setOverflowOpen] = useState(false); useEffect(() =>{ if (!fit.hasOverflow) setOverflowOpen(false); } , [fit.hasOverflow]); const isExternalSlot = (unit: NormalizedUnit): boolean => unit.kind === 'custom' && !renderCustom?.[unit.slot]; const renderUnitAt = ( unit: NormalizedUnit, variant: string, layer: 'live' | 'measure', ): React.ReactNode =>{ if (unit.kind === 'custom'){ const registered = renderCustom?.[unit.slot]; if (registered){ const custom = registered(variant, { layer, measure: (width) => onWidth(unitKey(unit, variant), width), } ); if (custom !== undefined) return custom; } else if (layer === 'live'){ const cmd = resolveCmd(unit.terminal); return ( <NativeSlotSocket name={ unit.slot} k={ unitKey(unit, variant)} onWidth={ onWidth} >{ cmd ? renderCommand(cmd, 'icon', () => executeCmd(unit.terminal)) : null} </NativeSlotSocket> ); } else{ return null; } const cmd = resolveCmd(unit.terminal); return cmd ? renderCommand(cmd, 'icon', () => executeCmd(unit.terminal)) : null; } const cmd = resolveCmd(unit.command); if (!cmd) return renderCommand({ id: unit.command, label: unit.command, shortcuts: [], enabled: false, active: false, visible: true, categories: [], } , variant, () =>{ } , ); return renderCommand(cmd, variant, () => executeCmd(unit.command)); } ; const collapsedView = (group: NormalizedGroup): CollapsedGroupView => ({ id: group.id, labelKey: group.labelKey, collapse: group.collapse ?? 'menu', role: group.role, commands: group.units .map((unit) => resolveCmd(commandOf(unit))) .filter((command): command is ResolvedCommand => command !== null && command.visible), execute: executeCmd, } ); const renderCollapsedGroup = (group: NormalizedGroup): React.ReactNode => renderCollapsed ? ( renderCollapsed(collapsedView(group)) ) : ( <DefaultCollapsed view={ collapsedView(group)} /> ); const disclosureView = (group: NormalizedGroup, allChildren: boolean): GroupDisclosureView => ({ id: group.id, labelKey: group.labelKey, role: group.role, commands: group.units .filter((unit) => allChildren || fit.units.get(unit.key)?.kind === 'shed') .map((unit) => resolveCmd(commandOf(unit))) .filter((command): command is ResolvedCommand => command !== null && command.visible), execute: executeCmd, } ); const renderDisclosure = (group: NormalizedGroup, allChildren: boolean): React.ReactNode => renderGroupTrigger ? ( renderGroupTrigger(disclosureView(group, allChildren)) ) : ( <DefaultGroupTrigger view={ disclosureView(group, allChildren)} /> ); const renderLiveGroup = (group: NormalizedGroup): React.ReactNode[] =>{ const assignment = fit.groups.get(group.id); if (!assignment || assignment.overflowed) return []; if (assignment.collapsed) return [<React.Fragment key={ group.id} >{ renderCollapsedGroup(group)} </React.Fragment>]; const nodes: React.ReactNode[] = []; for (const unit of group.units){ const unitAssignment = fit.units.get(unit.key); if (unitAssignment?.kind !== 'variant') continue; nodes.push( <React.Fragment key={ unit.key} >{ renderUnitAt(unit, unitAssignment.variant, 'live')} </React.Fragment>, ); } if (assignment.shedCount > 0){ nodes.push( <React.Fragment key={ `${ group.id} ::trigger`} >{ renderDisclosure(group, false)} </React.Fragment>, ); } return nodes.length ? [<React.Fragment key={ group.id} >{ nodes} </React.Fragment>] : []; } ; const renderLiveSection = (section: NormalizedSection | undefined): React.ReactNode =>{ if (!section) return null; const rendered = section.groups .map((group) => ({ id: group.id, nodes: renderLiveGroup(group)} )) .filter((group) => group.nodes.length > 0); return rendered.map((group, i) => ( <React.Fragment key={ group.id} >{ i > 0 && renderSeparator()} { group.nodes} </React.Fragment> )); } ; const sectionByName = (name: 'start' | 'center' | 'end') => visibleBar.sections.find((section) => section.name === name); const sectionStyle = (position: 'start' | 'center' | 'end'): React.CSSProperties => ({ display: 'flex', alignItems: 'center', gap, flex: '0 0 auto', ...(position === 'center' ?{ marginLeft: 'auto', marginRight: 'auto'} : null), } ); const overflowView: OverflowMenuView ={ sections: overflowSections, isOpen: overflowOpen, close: () => setOverflowOpen(false), resolve: resolveCmd, execute: executeCmd, } ; return ( <div ref={ containerRef} className={ className} style={ { position: 'relative', display: 'flex', alignItems: 'center', ...style} } > <div style={ sectionStyle('start')} >{ renderLiveSection(sectionByName('start'))} </div> <div style={ sectionStyle('center')} >{ renderLiveSection(sectionByName('center'))} </div> <div style={ sectionStyle('end')} >{ renderLiveSection(sectionByName('end'))} { fit.hasOverflow && ( <span style={ { position: 'relative', display: 'inline-flex'} } >{ renderOverflowTrigger(overflowOpen, () => setOverflowOpen((previous) => !previous))} { renderOverflowMenu ? ( renderOverflowMenu(overflowView) ) : ( <DefaultOverflowMenu view={ overflowView} /> )} </span> )} </div>{ } <div aria-hidden style={ measureLayerStyle} >{ visibleBar.sections.flatMap((section) => section.groups.flatMap((group) => [ ...group.units .filter((unit) => !isExternalSlot(unit)) .flatMap((unit) => unit.variants.map((variant) => ( <Measured key={ unitKey(unit, variant)} k={ unitKey(unit, variant)} onWidth={ onWidth} >{ renderUnitAt(unit, variant, 'measure')} </Measured> )), ), ...(group.collapse ? [ <Measured key={ groupKey(group.id)} k={ groupKey(group.id)} onWidth={ onWidth} >{ renderCollapsedGroup(group)} </Measured>, ] : []), ...(group.shed ? [ <Measured key={ groupTriggerKey(group.id)} k={ groupTriggerKey(group.id)} onWidth={ onWidth} >{ renderDisclosure(group, true)} </Measured>, ] : []), ]), )} <Measured k={ TRIGGER_KEY} onWidth={ onWidth} >{ renderOverflowTrigger(false, () =>{ } )} </Measured> </div> </div> ); }
ToolbarProps [InterfaceDeclaration]: export interface ToolbarProps{ bar: BarSchema; gap?: number; separatorWidth?: number; className?: string; style?: React.CSSProperties; renderCommand?: (cmd: ResolvedCommand, variant: string, run: () => void) => React.ReactNode; renderCustom?: Record<string, (variant: string, ctx: CustomSlotCtx) => React.ReactNode>; renderCollapsed?: (view: CollapsedGroupView) => React.ReactNode; renderGroupTrigger?: (view: GroupDisclosureView) => React.ReactNode; renderSeparator?: () => React.ReactNode; renderOverflowTrigger?: (isOpen: boolean, toggle: () => void) => React.ReactNode; renderOverflowMenu?: (view: OverflowMenuView) => React.ReactNode; }
UnitAssignment [TypeAliasDeclaration] (from packages/core/ui/src/solver.ts): export type UnitAssignment = |{ readonly kind: 'variant'; readonly variant: string} |{ readonly kind: 'shed'} |{ readonly kind: 'collapsed'} |{ readonly kind: 'overflow'} ;
Variant [TypeAliasDeclaration] (from packages/core/ui/src/schema.ts): export type Variant = 'icon+label' | 'icon' | 'label';
addItem [FunctionDeclaration] (from packages/core/ui/src/transforms.ts): export function addItem(schema: ChromeSchema, spec: AddItemSpec): ChromeSchema{ const hits = locate(schema, spec.bar); if (hits.length === 0) throw new Error(`[ui-core] addItem: no bar "${ spec.bar} " in bars/modeBars/strips`); if (hits.length > 1) throw new Error( `[ui-core] addItem: bar id "${ spec.bar} " is ambiguous (${ hits.map(([collection]) => collection).join(', ')} )`, ); const [coll, key] = hits[0]; const bar = schema[coll]![key]; for (const name of SECTION_NAMES){ if (name === spec.section) continue; if (bar.sections[name]?.some((group) => group.id === spec.group)) throw new Error( `[ui-core] addItem: group "${ spec.group} " lives in section "${ name} ", not "${ spec.section} "`, ); } const groups = bar.sections[spec.section] ?? []; const index = groups.findIndex((group) => group.id === spec.group); const nextGroups = index === -1 ? [...groups, makeGroup(spec.group, [spec.item])] : groups.map((group, i) =>{ if (i !== index) return group; const items = [...group.items]; items.splice(spec.at ?? items.length, 0, spec.item); return{ ...group, items} ; } ); return withBar(schema, coll, key, { ...bar, sections:{ ...bar.sections, [spec.section]: nextGroups} , } ); }
chromeHelpers [VariableDeclaration] (from packages/core/ui/src/helpers.ts): chromeHelpers ={ addItem, removeItems, replaceItem, item, group, custom, defineChrome, } as const
custom [FunctionDeclaration] (from packages/core/ui/src/schema.ts): export function custom(slot: string, options: Omit<CustomItem, 'slot'>): CustomItem{ return{ slot, ...options} ; }
defineChrome [FunctionDeclaration] (from packages/core/ui/src/schema.ts): export function defineChrome<T extends ChromeSchema>(chrome: T): T{ for (const bar of Object.values(chrome.bars)) normalizeBar(bar); for (const bar of Object.values(chrome.modeBars ??{ } )) normalizeBar(bar); for (const strip of Object.values(chrome.strips ??{ } )) normalizeBar(strip); return chrome; }
filterBar [FunctionDeclaration] (from packages/core/ui/src/schema.ts): export function filterBar( bar: NormalizedBar, keep: (unit: NormalizedUnit) => boolean, ): NormalizedBar{ return{ id: bar.id, sections: bar.sections.map((section) => ({ name: section.name, groups: section.groups.map((group) => ({ ...group, units: group.units.filter(keep)} )), } )), } ; }
formatShortcut [FunctionDeclaration] (from packages/core/ui/src/shortcuts.ts): export function formatShortcut(shortcut: string, options:{ isMac: boolean} ): string{ const parsed = parseShortcut(shortcut); const key = parsed.key.length === 1 ? parsed.key.toUpperCase() : capitalize(parsed.key); if (options.isMac){ const mods = [ parsed.ctrl ? '⌃' : '', parsed.alt ? '⌥' : '', parsed.shift ? '⇧' : '', parsed.meta || parsed.mod ? '⌘' : '', ].join(''); return `${ mods} ${ key} `; } const mods = [ parsed.ctrl || parsed.mod ? 'Ctrl' : '', parsed.meta ? 'Meta' : '', parsed.alt ? 'Alt' : '', parsed.shift ? 'Shift' : '', ].filter(Boolean); return [...mods, key].join('+'); }
group [FunctionDeclaration] (from packages/core/ui/src/schema.ts): export function group(id: string, items: readonly BarChild[]): BarGroup;
item [FunctionDeclaration] (from packages/core/ui/src/schema.ts): export function item(command: string, options?: Omit<BarItem, 'command'>): BarItem{ return{ command, ...options} ; }
matchShortcut [FunctionDeclaration] (from packages/core/ui/src/shortcuts.ts): export function matchShortcut( parsed: ParsedShortcut, stroke: KeyStroke, options:{ isMac: boolean} , ): boolean{ const wantCtrl = parsed.ctrl || (parsed.mod && !options.isMac); const wantMeta = parsed.meta || (parsed.mod && options.isMac); if (stroke.ctrlKey !== wantCtrl) return false; if (stroke.metaKey !== wantMeta) return false; if (stroke.altKey !== parsed.alt) return false; if (parsed.shift && !stroke.shiftKey) return false; if (!parsed.shift && stroke.shiftKey && parsed.key.length > 1) return false; const key = stroke.key.toLowerCase(); if (key === parsed.key) return true; return ( parsed.key.length > 1 && stroke.code !== undefined && stroke.code.toLowerCase() === parsed.key ); }
normalizeBar [FunctionDeclaration] (from packages/core/ui/src/schema.ts): export function normalizeBar(bar: BarSchema): NormalizedBar{ const seen = new Set<string>(); const groupIds = new Set<string>(); const sections: NormalizedSection[] = []; for (const name of ['start', 'center', 'end'] as const){ const groups = bar.sections[name]; if (!groups || groups.length === 0) continue; sections.push({ name, groups: groups.map((group) =>{ if (groupIds.has(group.id)) throw new Error(`[ui-core] duplicate group in bar: ${ group.id} `); groupIds.add(group.id); return{ id: group.id, labelKey: group.labelKey, role: group.role ?? 'buttons', shed: group.shed ?? false, collapse: group.collapse, importance: group.importance ?? DEFAULT_IMPORTANCE, units: group.items.map((child) => normalizeChild(group.id, child, seen)), } ; } ), } ); } return{ id: bar.id, sections} ; }
parseShortcut [FunctionDeclaration] (from packages/core/ui/src/shortcuts.ts): export function parseShortcut(shortcut: string): ParsedShortcut{ const tokens = shortcut.split('+').map((token) => token.trim()); const keyToken = tokens[tokens.length - 1] === '' ? '+' : tokens[tokens.length - 1]; const parsed ={ key: keyToken.toLowerCase(), mod: false, ctrl: false, meta: false, alt: false, shift: false, } ; for (const token of tokens.slice(0, -1)){ const modifier = token.toLowerCase(); if (modifier === '') continue; if (!MODIFIERS.has(modifier)) throw new Error(`[ui-core] unknown modifier "${ token} " in "${ shortcut} "`); if (modifier === 'mod') parsed.mod = true; else if (modifier === 'ctrl' || modifier === 'control') parsed.ctrl = true; else if (modifier === 'meta' || modifier === 'cmd' || modifier === 'command') parsed.meta = true; else if (modifier === 'alt' || modifier === 'option') parsed.alt = true; else parsed.shift = true; } if (parsed.key === '') throw new Error(`[ui-core] empty key in shortcut "${ shortcut} "`); return parsed; }
projectOverflow [FunctionDeclaration] (from packages/core/ui/src/projection.ts): export function projectOverflow( bar: NormalizedBar, fit: FitResult, resolve: ResolveMenuTarget, ): OverflowSection[]{ const sections: OverflowSection[] = []; for (const section of bar.sections){ for (const group of section.groups){ const rows = group.units .filter((unit) => fit.units.get(unit.key)?.kind === 'overflow') .map((unit) => projectUnit(unit, resolve)); if (rows.length === 0) continue; sections.push({ labelKey: group.labelKey, role: group.role === 'tabs' ? 'radio' : undefined, rows, } ); } } return sections; }
projectShed [FunctionDeclaration] (from packages/core/ui/src/projection.ts): export function projectShed( group: NormalizedGroup, fit: FitResult, resolve: ResolveMenuTarget, ): OverflowRow[]{ return group.units .filter((unit) => fit.units.get(unit.key)?.kind === 'shed') .map((unit) => projectUnit(unit, resolve)); }
projectStrip [FunctionDeclaration] (from packages/core/ui/src/projection.ts): export function projectStrip( bar: NormalizedBar, visible: (command: string) => boolean, ): StripGroup[]{ const groups: StripGroup[] = []; for (const section of bar.sections){ for (const group of section.groups){ const commands = group.units .map((unit) => (unit.kind === 'custom' ? unit.terminal : unit.command)) .filter(visible); if (commands.length === 0) continue; groups.push({ id: group.id, labelKey: group.labelKey, commands} ); } } return groups; }
removeItems [FunctionDeclaration] (from packages/core/ui/src/transforms.ts): export function removeItems(schema: ChromeSchema, ids: readonly string[]): ChromeSchema{ const gone = new Set(ids); const keep = (child: BarChild): boolean =>{ const cmd = commandOf(child); return cmd !== null ? !gone.has(cmd) : !gone.has((child as CustomItem).slot); } ; const filterGroups = (groups: readonly BarGroup[] | undefined) => groups ?.map((group) => ({ ...group, items: group.items.filter(keep)} )) .filter((group) => group.items.length > 0); const filterBarSchema = (bar: BarSchema): BarSchema => ({ ...bar, sections: Object.fromEntries( SECTION_NAMES.flatMap((name) =>{ const groups = filterGroups(bar.sections[name]); return groups && groups.length > 0 ? [[name, groups]] : []; } ), ), } ); const filterColl = <T extends Readonly<Record<string, BarSchema>> | undefined>(coll: T): T => coll && (Object.fromEntries( Object.entries(coll).map(([key, bar]) => [key, filterBarSchema(bar)]), ) as T); return{ ...schema, bars: filterColl(schema.bars)!, modeBars: filterColl(schema.modeBars), strips: filterColl(schema.strips), menus: schema.menus && Object.fromEntries( Object.entries(schema.menus).map(([key, menu]) => [ key, { ...menu, sections: menu.sections .map((section) => ({ ...section, items: section.items.filter((command) => !gone.has(command)), } )) .filter((section) => section.items.length > 0), } , ]), ), } ; }
replaceItem [FunctionDeclaration] (from packages/core/ui/src/transforms.ts): export function replaceItem(schema: ChromeSchema, id: string, item: BarChild): ChromeSchema{ const replacementCommand = commandOf(item); const swap = (child: BarChild): BarChild => (commandOf(child) === id ? item : child); const mapBar = (bar: BarSchema): BarSchema => ({ ...bar, sections: Object.fromEntries( SECTION_NAMES.flatMap((name) =>{ const groups = bar.sections[name]; return groups ? [[name, groups.map((group) => ({ ...group, items: group.items.map(swap)} ))]] : []; } ), ), } ); const mapColl = <T extends Readonly<Record<string, BarSchema>> | undefined>(coll: T): T => coll && (Object.fromEntries(Object.entries(coll).map(([key, bar]) => [key, mapBar(bar)])) as T); return{ ...schema, bars: mapColl(schema.bars)!, modeBars: mapColl(schema.modeBars), strips: mapColl(schema.strips), menus: replacementCommand === null ? schema.menus : schema.menus && Object.fromEntries( Object.entries(schema.menus).map(([key, menu]) => [ key, { ...menu, sections: menu.sections.map((section) => ({ ...section, items: section.items.map((command) => command === id ? replacementCommand : command, ), } )), } , ]), ), } ; }
solve [FunctionDeclaration] (from packages/core/ui/src/solver.ts): export function solve(bar: NormalizedBar, metrics: FitMetrics, containerWidth: number): FitResult{ let pass = solvePass(bar, metrics, containerWidth, false); if (anyOverflow(pass.groups)){ pass = solvePass(bar, metrics, containerWidth, true); } const units = new Map<string, UnitAssignment>(); const groupAssignments = new Map<string, GroupAssignment>(); for (const gs of pass.groups){ const shedCount = gs.units.filter( (unit) => unit.shed && !unit.overflowed && !gs.collapsed, ).length; groupAssignments.set(gs.group.id, { shedCount, collapsed: gs.collapsed, overflowed: gs.overflowed, } ); for (const us of gs.units){ units.set( us.unit.key, us.overflowed ?{ kind: 'overflow'} : gs.collapsed ?{ kind: 'collapsed'} : us.shed ?{ kind: 'shed'} :{ kind: 'variant', variant: us.unit.variants[us.variantIndex]} , ); } } return{ units, groups: groupAssignments, hasOverflow: anyOverflow(pass.groups), width: pass.measured.width, complete: pass.measured.complete, } ; }
useStripView [FunctionDeclaration]: export function useStripView(bar: BarSchema | undefined): StripView | null{ const commands = useCapability(CommandsToken); const documentId = useDocumentId(); const normalized = useMemo(() => (bar ? normalizeBar(bar) : null), [bar]); const groups = useKernelValue(() =>{ if (!normalized) return NO_STRIP_GROUPS; const resolved = new Map<string, ResolvedCommand>(); const visible = (id: string) =>{ const cmd = commands.resolveCommand(id, documentId ?? undefined); if (cmd) resolved.set(id, cmd); return cmd?.visible === true; } ; return projectStrip(normalized, visible).map((group) => ({ id: group.id, labelKey: group.labelKey, commands: group.commands.map((id) => resolved.get(id)!), } )); } , stripGroupsEqual); return useMemo( () => groups.length === 0 ? null :{ groups, execute: (id: string) => void commands.execute(id, { documentId: documentId ?? undefined} ), } , [groups, commands, documentId], ); }
validateChrome [FunctionDeclaration] (from packages/core/ui/src/schema.ts): export function validateChrome(chrome: ChromeSchema, knownCommands: ReadonlySet<string>): string[]{ const problems: string[] = []; const checkBar = (bar: BarSchema, where: string) =>{ for (const section of normalizeBar(bar).sections) for (const group of section.groups) for (const unit of group.units){ const refs = unit.kind === 'command' ? [unit.command] : [unit.terminal]; for (const command of refs) if (!knownCommands.has(command)) problems.push(`${ where} : unknown command "${ command} "`); } } ; for (const [id, bar] of Object.entries(chrome.bars)) checkBar(bar, `bars.${ id} `); for (const [id, bar] of Object.entries(chrome.modeBars ??{ } )) checkBar(bar, `modeBars.${ id} `); for (const [id, strip] of Object.entries(chrome.strips ??{ } )) checkBar(strip, `strips.${ id} `); for (const [id, menu] of Object.entries(chrome.menus ??{ } )) for (const section of menu.sections) for (const command of section.items) if (!knownCommands.has(command)) problems.push(`menus.${ id} : unknown command "${ command} "`); return problems; }
