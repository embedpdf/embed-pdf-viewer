/** @embedpdf/plugin-link/contract/host — the HOST lens (`isNavigationEngaged`). Same runtime token. */
export * from './contract';
export { LinkToken } from './types';
export type { LinkAction, LinkHostCapability, LinkState } from './types';
